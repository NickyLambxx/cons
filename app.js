/* Интерактивная Конституция — Защищенная версия */
const $ = (s, r = document) => r.querySelector(s);
const $$ = (s, r = document) => Array.from(r.querySelectorAll(s));

function safeAddListener(selector, event, handler) {
    const el = $(selector);
    if (el) el.addEventListener(event, handler);
}

function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}

const state = {
    showFavoritesOnly: false,
    articles: [],
    favorites: new Set(),
    favFolders: ['General'],
    articleFolders: {}, 
    currentFolderFilter: 'all',
    notes: {}, 
    returnPosition: null,
    landingPosition: null,
    isJumping: false,
    fontSize: 16,
    lineHeight: 1.6,
    searchHistory: [],
    progress: {},
    activeSearchQuery: '',
    mapZoom: 1,
    mapPan: { x: 0, y: 0 }
};

const LS = {
    THEME: 'ic-theme',
    FAVORITES: 'ic-favorites',
    FAV_FOLDERS: 'ic-fav-folders-list',
    ARTICLE_FOLDERS: 'ic-article-folders-map',
    NOTES: 'ic-user-notes',
    FONT: 'ic-font-settings',
    FONT_TYPE: 'ic-font-type',
    HIGHSCORE: 'ic-game-highscore',
    SEARCH: 'ic-search-history',
    PROGRESS: 'ic-chapter-progress',
    CACHE_CHAPTERS: 'ic-chapters-cache'
};

// --- СЛОВАРЬ ТЕРМИНОВ ---
const DICTIONARY = {
    "адвокат": "Квалифицированный юрист, оказывающий профессиональную юридическую помощь физическим и юридическим лицам. Конституция гарантирует каждому право на получение квалифицированной юридической помощи (ст. 48).",
    "адвокатура": "Профессиональное сообщество адвокатов, независимое от государства. Действует на основе принципов законности, независимости, самоуправления и корпоративности; относится к совместному ведению РФ и субъектов (ст. 72).",
    "амнистия": "Освобождение от уголовной ответственности или смягчение наказания в отношении неопределённого круга лиц. Объявляется постановлением Государственной Думы (ст. 103 Конституции РФ).",
    "безопасность": "Состояние защищённости жизненно важных интересов личности, общества и государства от внутренних и внешних угроз. Относится к исключительному ведению Российской Федерации (ст. 71).",
    "бюджет": "Федеральный бюджет — ежегодный финансовый план доходов и расходов государства, утверждаемый в форме федерального закона. Разрабатывается Правительством и представляется в Государственную Думу (ст. 114).",
    "валюта": "Денежная единица государства. В РФ единственным законным платёжным средством является рубль. Денежная эмиссия осуществляется исключительно Центральным банком (ст. 75).",
    "вероисповедание": "Принадлежность лица к определённой религии или отказ от любой религии. В РФ гарантирована свобода совести и вероисповедания, включая право исповедовать любую религию или не исповедовать никакой (ст. 28).",
    "власть": "Способность субъекта подчинять поведение других лиц своей воле. В РФ государственная власть делится на законодательную, исполнительную и судебную; органы каждой ветви самостоятельны (ст. 10).",
    "вотум недоверия": "Выражение Государственной Думой недоверия Правительству РФ. При повторном вотуме в течение трёх месяцев Президент принимает решение об отставке Правительства или роспуске Думы (ст. 117).",
    "выборы": "Форма непосредственного волеизъявления граждан при формировании органов государственной власти и местного самоуправления. Выборы в РФ — свободные, прямые, тайные (ст. 3, 32).",
    "гражданское общество": "Совокупность негосударственных объединений, организаций и отношений, обеспечивающих реализацию интересов граждан независимо от государства. Является условием существования правового государства.",
    "гражданство": "Устойчивая правовая связь физического лица с Российской Федерацией, выражающаяся во взаимных правах, обязанностях и ответственности. Гражданство РФ является единым и равным независимо от оснований приобретения (ст. 6).",
    "дееспособность": "Способность гражданина своими действиями приобретать и осуществлять права, исполнять обязанности. В полном объёме возникает с 18 лет. Может быть ограничена или прекращена только судом.",
    "демократия": "Форма государственного устройства, при которой власть принадлежит народу и осуществляется им непосредственно или через выборных представителей. РФ является демократическим государством (ст. 1).",
    "депутат": "Избранный представитель народа, входящий в состав законодательного органа. Депутаты Государственной Думы избираются на 5 лет; обладают парламентской неприкосновенностью (ст. 97, 98).",
    "договор": "Соглашение двух или более сторон, порождающее правовые последствия. Международные договоры РФ являются составной частью её правовой системы и имеют приоритет над федеральными законами (ст. 15).",
    "думе": "Государственная Дума — нижняя палата Федерального Собрания РФ. Состоит из 450 депутатов, избираемых на 5 лет. Принимает федеральные законы, утверждает бюджет, назначает ряд должностных лиц (ст. 95—103).",
    "жилище": "Место постоянного или временного проживания гражданина. Неприкосновенность жилища гарантируется Конституцией: никто не вправе проникнуть против воли проживающих без судебного решения (ст. 25).",
    "закон": "Нормативный правовой акт, принятый законодательным органом в установленном порядке и обладающий высшей юридической силой (после Конституции). Федеральные законы обязательны на всей территории РФ (ст. 15, 76).",
    "здоровье": "Состояние физического и психического благополучия человека. Каждый вправе на охрану здоровья и медицинскую помощь; в государственных и муниципальных учреждениях она оказывается бесплатно (ст. 41).",
    "импичмент": "Процедура досрочного отстранения Президента РФ от должности за государственную измену или иное тяжкое преступление. Обвинение выдвигает Государственная Дума, отрешает — Совет Федерации (ст. 93).",
    "имущество": "Совокупность материальных ценностей и прав, принадлежащих субъекту. В РФ признаются и защищаются частная, государственная, муниципальная и иные формы собственности (ст. 8, 35).",
    "конституционный суд": "Высший судебный орган конституционного контроля в РФ. Проверяет соответствие законов Конституции, разрешает споры о компетенции органов власти, толкует Конституцию (ст. 125).",
    "конституция": "Основной закон государства, имеющий высшую юридическую силу, прямое действие и применяемый на всей территории РФ. Законы и иные правовые акты не должны противоречить Конституции (ст. 15).",
    "лицензирование": "Разрешительный порядок осуществления определённых видов деятельности, при котором государство выдаёт специальное разрешение — лицензию. Относится к инструментам государственного регулирования экономики.",
    "местное самоуправление": "Форма осуществления народом своей власти, обеспечивающая самостоятельное и ответственное решение населением вопросов местного значения. Не входит в систему органов государственной власти (ст. 12, 130).",
    "монархия": "Форма правления, при которой верховная государственная власть сосредоточена (полностью или частично) в руках единоличного главы государства — монарха, получающего власть по наследству.",
    "муниципальное образование": "Территориальная единица, в границах которой осуществляется местное самоуправление: городское или сельское поселение, муниципальный район, городской округ и др. (ст. 131).",
    "налоги": "Обязательные безвозмездные платежи, взимаемые государством с граждан и организаций. Каждый обязан платить законно установленные налоги и сборы; незаконные налоги незаконны (ст. 57).",
    "наследство": "Переход имущества, прав и обязанностей умершего (наследодателя) к другим лицам (наследникам). Право наследования гарантируется Конституцией РФ (ст. 35).",
    "нотариат": "Система органов и должностных лиц, уполномоченных удостоверять сделки, факты и документы в целях обеспечения их юридической силы. Относится к совместному ведению РФ и субъектов (ст. 72).",
    "оборона": "Система политических, экономических и военных мер по защите государства от внешнего нападения. Находится в исключительном ведении РФ; Президент является Верховным Главнокомандующим (ст. 71, 87).",
    "образование": "Единый целенаправленный процесс воспитания и обучения. Основное общее образование в РФ обязательно; каждый вправе на конкурсной основе получить высшее образование в государственном вузе бесплатно (ст. 43).",
    "омбудсмен": "Уполномоченный по правам человека в РФ — независимое должностное лицо, назначаемое Государственной Думой. Рассматривает жалобы граждан на нарушения их прав государственными органами (ст. 103).",
    "охрана": "Деятельность государства по защите прав и свобод граждан, природной среды, здоровья населения и других ценностей. Охрана окружающей среды — предмет совместного ведения РФ и субъектов (ст. 9, 41, 72).",
    "пенсия": "Регулярная денежная выплата гражданам при наступлении страхового случая (старость, инвалидность, потеря кормильца). Государственные пенсии и пособия устанавливаются законом (ст. 39).",
    "политическое убежище": "Право иностранных граждан и лиц без гражданства на защиту в РФ по причине преследования за убеждения или действия в соответствии с общепризнанными принципами международного права. Предоставляется Президентом (ст. 63, 89).",
    "помилование": "Акт Президента РФ, освобождающий конкретное осуждённое лицо от дальнейшего отбывания наказания или смягчающий наказание. Применяется индивидуально (в отличие от амнистии) (ст. 89).",
    "пошлины": "Денежные сборы, взимаемые государством за совершение юридически значимых действий или за ввоз/вывоз товаров через границу. Таможенное регулирование — исключительное ведение РФ (ст. 71, 74).",
    "правительство": "Высший исполнительный орган государственной власти РФ. Состоит из Председателя, заместителей и федеральных министров; руководит деятельностью федеральных органов исполнительной власти (ст. 110).",
    "правовое государство": "Государство, в котором верховенство закона и Конституции обеспечивается реально: права человека признаются высшей ценностью, власть ограничена правом, существует разделение властей (ст. 1).",
    "правоспособность": "Способность гражданина иметь права и нести обязанности. Возникает с момента рождения и прекращается смертью. Ограничить правоспособность может только закон или суд.",
    "правосубъектность": "Совокупность правоспособности и дееспособности; способность лица быть субъектом правоотношений. Для юридических лиц возникает с момента государственной регистрации.",
    "правосудие": "Деятельность судов по рассмотрению и разрешению уголовных, гражданских, административных и иных дел в установленном законом процессуальном порядке. В РФ осуществляется только судом (ст. 118).",
    "президент": "Глава государства, гарант Конституции РФ, прав и свобод человека и гражданина. Избирается на 6 лет, не более двух сроков. Определяет основные направления внутренней и внешней политики (ст. 80).",
    "присяга": "Торжественное публичное клятвенное обещание. Президент РФ приносит присягу народу при вступлении в должность (ст. 82); судьи и иные должностные лица также приносят присягу.",
    "прокуратура": "Единая федеральная централизованная система органов, осуществляющих надзор за соблюдением Конституции и исполнением законов. Возглавляет Генеральный прокурор РФ (ст. 129).",
    "республика": "Форма правления, при которой высшие органы государственной власти являются выборными и сменяемыми. РФ имеет республиканскую форму правления (ст. 1). Некоторые субъекты РФ тоже называются республиками (ст. 65).",
    "республиканская": "Характеристика формы правления РФ: высшие органы власти (Президент, Государственная Дума) являются выборными и сменяемыми (ст. 1).",
    "референдум": "Высшая форма непосредственного волеизъявления граждан РФ — всенародное голосование по наиболее важным вопросам государственной жизни. Является высшим выражением власти народа наряду с выборами (ст. 3).",
    "светское государство": "Государство, в котором никакая религия не является государственной или обязательной, религиозные объединения отделены от государства и равны перед законом. РФ является светским государством (ст. 14).",
    "светское": "Характеристика РФ как государства, в котором религия отделена от государства; никакая религия не может устанавливаться в качестве государственной или обязательной (ст. 14).",
    "свобода": "Возможность человека действовать по собственному усмотрению в сферах, не ограниченных законом. В Конституции закреплены свобода слова, совести, передвижения, творчества и другие (Глава 2).",
    "семья": "Основная ячейка общества, находящаяся под защитой государства. Конституция обязывает государство поддерживать материнство, отцовство, детство и семью; оба родителя обязаны заботиться о детях (ст. 38).",
    "собственность": "Право владеть, пользоваться и распоряжаться имуществом. В РФ признаются и равно защищаются частная, государственная, муниципальная и иные формы собственности (ст. 8, 35).",
    "собрание": "Федеральное Собрание — представительный и законодательный орган (парламент) РФ; состоит из Совета Федерации и Государственной Думы (ст. 94). Также право граждан собираться мирно (ст. 31).",
    "совет федерации": "Верхняя палата Федерального Собрания РФ. Формируется из представителей каждого субъекта РФ (по одному от законодательного и исполнительного органов). Рассматривает и одобряет (или отклоняет) законы (ст. 95—102).",
    "социальное государство": "Государство, политика которого направлена на создание условий, обеспечивающих достойную жизнь и свободное развитие каждого человека. РФ является социальным государством (ст. 7).",
    "социальное": "Характеристика РФ как государства, политика которого обеспечивает достойную жизнь граждан: охрана труда, МРОТ, поддержка семьи, пенсии, социальные службы (ст. 7).",
    "суверенитет": "Верховенство и независимость государственной власти внутри страны и на международной арене. Суверенитет РФ распространяется на всю её территорию (ст. 4).",
    "суд": "Орган, осуществляющий правосудие в РФ. Судопроизводство осуществляется на принципах состязательности, равноправия сторон, независимости судей (ст. 118, 120).",
    "судья": "Лицо, наделённое полномочиями осуществлять правосудие. Судьи независимы и подчиняются только Конституции и федеральному закону; несменяемы и неприкосновенны (ст. 120—122).",
    "таможня": "Государственный орган, осуществляющий контроль за перемещением товаров и лиц через государственную границу. Таможенное регулирование — исключительное ведение РФ (ст. 71, 74).",
    "труд": "Целенаправленная деятельность человека. Труд в РФ свободен; принудительный труд запрещён. Каждый вправе свободно распоряжаться своими способностями к труду (ст. 37).",
    "убежище политическое": "Право иностранных граждан и лиц без гражданства на защиту в РФ от преследования по политическим мотивам в их стране. Предоставляется Президентом РФ (ст. 63, 89).",
    "указ": "Нормативный или индивидуальный правовой акт, издаваемый Президентом РФ. Указы обязательны для исполнения на всей территории страны и не должны противоречить Конституции и федеральным законам (ст. 90).",
    "унитарное государство": "Форма государственного устройства, при которой государство не имеет в своём составе федеративных единиц. Административно-территориальные единицы подчиняются центральной власти (в отличие от РФ — федерации).",
    "федерация": "Форма государственного устройства, при которой государство состоит из субъектов (республики, края, области и т.д.), имеющих собственные органы власти. РФ является федеративным государством (ст. 1, 5).",
    "экология": "Совокупность вопросов, связанных с охраной окружающей природной среды. Каждый имеет право на благоприятную окружающую среду и возмещение ущерба от экологических нарушений (ст. 42).",
    "экстрадиция": "Выдача лица, совершившего преступление, другому государству для привлечения к ответственности. Конституция запрещает выдачу российских граждан иностранным государствам (ст. 61).",
    "эмиссия": "Выпуск в обращение денежных знаков. В РФ денежная эмиссия — исключительное полномочие Центрального банка; осуществление эмиссии другими органами запрещено (ст. 75).",
    "ратификация": "Утверждение высшим органом государственной власти международного договора, подписанного уполномоченным лицом. В РФ ратификация осуществляется Государственной Думой в форме федерального закона (ст. 106).",
    "федеральное собрание": "Парламент России — постоянно действующий представительный и законодательный орган РФ. Состоит из двух палат: Совета Федерации (верхняя) и Государственной Думы (нижняя) (ст. 94–95).",
    "чрезвычайное положение": "Особый правовой режим деятельности органов власти, допускаемый в условиях непосредственной угрозы конституционному строю или безопасности граждан. Вводится указом Президента с незамедлительным уведомлением Совета Федерации (ст. 56, 88).",
    "военное положение": "Особый правовой режим, вводимый Президентом на территории РФ или её части при агрессии либо непосредственной угрозе агрессии. Указ о введении незамедлительно передаётся на утверждение Совета Федерации (ст. 87).",
    "конституционное собрание": "Особый орган, созываемый для пересмотра положений глав 1, 2 и 9 Конституции РФ. Может подтвердить неизменность Конституции, разработать новый проект или вынести его на всенародное голосование (ст. 135).",
    "вето": "Право главы государства отклонить принятый парламентом закон и вернуть его на повторное рассмотрение. Президент РФ вправе отклонить федеральный закон; Государственная Дума и Совет Федерации могут преодолеть вето двумя третями голосов (ст. 107).",
    "разделение властей": "Принцип организации государственной власти, при котором законодательная, исполнительная и судебная власти самостоятельны и независимы друг от друга. Закреплён как основа конституционного строя РФ (ст. 10).",
    "прожиточный минимум": "Стоимостная оценка потребительской корзины, а также обязательные платежи и сборы. Минимальный размер оплаты труда не может быть ниже прожиточного минимума трудоспособного населения (ст. 7, 421 ТК).",
    "конституционный строй": "Система общественных отношений, урегулированных нормами Конституции и составляющих основу государственного устройства РФ. Положения главы 1 Конституции о конституционном строе не могут быть пересмотрены Федеральным Собранием (ст. 16)."
};


/* --- ДАННЫЕ ДЛЯ КАРТЫ --- */
const FEDERAL_DISTRICTS = {
    "reg-cen": {
        title: "Центральный ФО",
        list: "Москва, Московская область, Белгородская область, Брянская область, Владимирская область, Воронежская область, Ивановская область, Калужская область, Костромская область, Курская область, Липецкая область, Орловская область, Рязанская область, Смоленская область, Тамбовская область, Тверская область, Тульская область, Ярославская область"
    },
    "reg-nw": {
        title: "Северо-Западный ФО",
        list: "Санкт-Петербург, Ленинградская область, Архангельская область, Вологодская область, Калининградская область, Мурманская область, Новгородская область, Псковская область, Республика Карелия, Республика Коми, Ненецкий АО"
    },
    "reg-south": {
        title: "Южный ФО",
        list: "Краснодарский край, Астраханская область, Волгоградская область, Ростовская область, Республика Адыгея, Республика Калмыкия, Республика Крым, Севастополь"
    },
    "reg-kav": {
        title: "Северо-Кавказский ФО",
        list: "Ставропольский край, Республика Дагестан, Республика Ингушетия, Кабардино-Балкарская Республика, Карачаево-Черкесская Республика, Северная Осетия — Алания, Чеченская Республика"
    },
    "reg-vol": {
        title: "Приволжский ФО",
        list: "Татарстан, Башкортостан, Чувашия, Пермский край, Нижегородская область, Самарская область, Саратовская область, Ульяновская область, Пензенская область, Оренбургская область, Кировская область, Марий Эл, Мордовия, Удмуртия"
    },
    "reg-ural": {
        title: "Уральский ФО",
        list: "Свердловская область, Челябинская область, Курганская область, Тюменская область, Ханты-Мансийский АО, Ямало-Ненецкий АО"
    },
    "reg-sib": {
        title: "Сибирский ФО",
        list: "Новосибирская область, Омская область, Томская область, Кемеровская область, Иркутская область, Красноярский край, Алтайский край, Республика Алтай, Тыва, Хакасия"
    },
    "reg-fe": {
        title: "Дальневосточный ФО",
        list: "Приморский край, Хабаровский край, Амурская область, Магаданская область, Сахалинская область, Якутия, Бурятия, Забайкальский край, Еврейская АО, Чукотский АО, Камчатский край"
    }
};

/* --- ИГРА №13 (РАСШИРЕННАЯ БАЗА) --- */
const POWERS = [
    { text: "Назначение выборов Государственной Думы", target: "president" },
    { text: "Роспуск Государственной Думы", target: "president" },
    { text: "Назначение референдума", target: "president" },
    { text: "Внесение законопроектов в Государственную Думу", target: "president" },
    { text: "Подписание и обнародование федеральных законов", target: "president" },
    { text: "Ежегодное обращение к Федеральному Собранию", target: "president" },
    { text: "Осуществление помилования", target: "president" },
    { text: "Решение вопросов гражданства РФ", target: "president" },
    { text: "Предоставление политического убежища", target: "president" },
    { text: "Награждение государственными наградами", target: "president" },
    { text: "Присвоение почетных званий", target: "president" },
    { text: "Присвоение высших воинских званий", target: "president" },
    { text: "Назначение Председателя Правительства РФ (с согласия ГД)", target: "president" },
    { text: "Принятие отставки Правительства РФ", target: "president" },
    { text: "Назначение федеральных министров", target: "president" },
    { text: "Руководство внешней политикой РФ", target: "president" },
    { text: "Ведение переговоров и подписание международных договоров", target: "president" },
    { text: "Назначение послов (после консультаций с парламентом)", target: "president" },
    { text: "Утверждение военной доктрины", target: "president" },
    { text: "Является Верховным Главнокомандующим", target: "president" },
    { text: "Введение военного положения", target: "president" },
    { text: "Введение чрезвычайного положения", target: "president" },
    { text: "Назначение судей федеральных судов (кроме высших)", target: "president" },
    { text: "Представление Совету Федерации кандидатур судей высших судов", target: "president" },
    { text: "Представление Совету Федерации кандидатуры Генпрокурора", target: "president" },
    { text: "Формирование Государственного Совета", target: "president" },
    { text: "Формирование Совета Безопасности", target: "president" },
    { text: "Назначение Администрации Президента", target: "president" },
    { text: "Утверждение изменения границ между субъектами РФ", target: "sf" },
    { text: "Утверждение указа Президента о введении военного положения", target: "sf" },
    { text: "Утверждение указа Президента о введении чрезвычайного положения", target: "sf" },
    { text: "Решение вопроса о возможности использования ВС РФ за пределами территории", target: "sf" },
    { text: "Назначение выборов Президента РФ", target: "sf" },
    { text: "Отрешение Президента РФ от должности", target: "sf" },
    { text: "Назначение судей Конституционного Суда РФ", target: "sf" },
    { text: "Назначение судей Верховного Суда РФ", target: "sf" },
    { text: "Назначение Генерального прокурора РФ (по представлению Президента)", target: "sf" },
    { text: "Назначение заместителей Генерального прокурора", target: "sf" },
    { text: "Назначение Председателя Счетной палаты", target: "sf" },
    { text: "Дача согласия Президенту на назначение Председателя Правительства", target: "gd" },
    { text: "Утверждение кандидатур заместителей Председателя Правительства", target: "gd" },
    { text: "Утверждение кандидатур федеральных министров (не силовых)", target: "gd" },
    { text: "Решение вопроса о доверии Правительству РФ", target: "gd" },
    { text: "Назначение Председателя Центрального банка РФ", target: "gd" },
    { text: "Освобождение от должности Председателя Центрального банка", target: "gd" },
    { text: "Назначение заместителя Председателя Счетной палаты", target: "gd" },
    { text: "Назначение Уполномоченного по правам человека", target: "gd" },
    { text: "Объявление амнистии", target: "gd" },
    { text: "Выдвижение обвинения против Президента РФ", target: "gd" },
    { text: "Разработка федерального бюджета", target: "gov" },
    { text: "Представление федерального бюджета Государственной Думе", target: "gov" },
    { text: "Обеспечение исполнения федерального бюджета", target: "gov" },
    { text: "Представление отчета об исполнении бюджета", target: "gov" },
    { text: "Обеспечение проведения единой финансовой политики", target: "gov" },
    { text: "Проведение единой политики в области культуры", target: "gov" },
    { text: "Проведение единой политики в области науки и образования", target: "gov" },
    { text: "Проведение единой политики в области здравоохранения", target: "gov" },
    { text: "Проведение единой политики в области социального обеспечения", target: "gov" },
    { text: "Проведение единой политики в области экологии", target: "gov" },
    { text: "Управление федеральной собственностью", target: "gov" },
    { text: "Осуществление мер по обеспечению обороны страны", target: "gov" },
    { text: "Осуществление мер по обеспечению государственной безопасности", target: "gov" },
    { text: "Поддержка добровольческой (волонтерской) деятельности", target: "gov" },
    { text: "Содействие развитию предпринимательства", target: "gov" },

    // --- Дополнительные вопросы — Президент ---
    { text: "Приостановление актов органов исполнительной власти субъектов РФ (до решения суда)", target: "president" },
    { text: "Председательствование на заседаниях Правительства РФ", target: "president" },
    { text: "Назначение и освобождение полномочных представителей Президента", target: "president" },
    { text: "Назначение и освобождение высшего командования Вооружённых Сил", target: "president" },
    { text: "Присвоение высших дипломатических рангов", target: "president" },
    { text: "Отмена постановлений Правительства, противоречащих Конституции", target: "president" },
    { text: "Обеспечение согласованного функционирования органов государственной власти", target: "president" },
    { text: "Определение основных направлений внутренней политики", target: "president" },
    { text: "Определение основных направлений внешней политики", target: "president" },
    { text: "Подписание ратификационных грамот", target: "president" },
    { text: "Принятие верительных и отзывных грамот дипломатических представителей", target: "president" },
    { text: "Осуществление руководства внешней политикой РФ", target: "president" },

    // --- Дополнительные вопросы — Совет Федерации ---
    { text: "Рассмотрение федерального конституционного закона об изменении Конституции", target: "sf" },
    { text: "Принятие регламента Совета Федерации", target: "sf" },
    { text: "Избрание Председателя Совета Федерации", target: "sf" },
    { text: "Формирование комитетов Совета Федерации", target: "sf" },
    { text: "Назначение заместителей Председателя Счётной палаты и аудиторов (по представлению)", target: "sf" },
    { text: "Одобрение или отклонение федерального закона, принятого Государственной Думой", target: "sf" },
    { text: "Рассмотрение законов по вопросам войны и мира", target: "sf" },
    { text: "Рассмотрение законов о федеральном бюджете", target: "sf" },
    { text: "Рассмотрение законов о ратификации международных договоров", target: "sf" },
    { text: "Назначение членов Центральной избирательной комиссии (по представлению Президента)", target: "sf" },

    // --- Дополнительные вопросы — Государственная Дума ---
    { text: "Принятие федеральных законов", target: "gd" },
    { text: "Принятие федерального бюджета (в форме федерального закона)", target: "gd" },
    { text: "Принятие регламента Государственной Думы", target: "gd" },
    { text: "Избрание Председателя Государственной Думы", target: "gd" },
    { text: "Формирование комитетов Государственной Думы", target: "gd" },
    { text: "Заслушивание ежегодного доклада Центрального банка", target: "gd" },
    { text: "Рассмотрение кандидатуры Председателя Центрального банка", target: "gd" },
    { text: "Назначение членов Центральной избирательной комиссии", target: "gd" },
    { text: "Выражение недоверия Правительству РФ", target: "gd" },
    { text: "Рассмотрение ежегодного отчёта Правительства РФ", target: "gd" },
    { text: "Назначение на должность и освобождение Уполномоченного по правам человека", target: "gd" },
    { text: "Выдвижение обвинения против Президента РФ (импичмент)", target: "gd" },

    // --- Дополнительные вопросы — Правительство ---
    { text: "Разработка прожиточного минимума и потребительской корзины", target: "gov" },
    { text: "Руководство деятельностью федеральных органов исполнительной власти", target: "gov" },
    { text: "Осуществление мер по государственной безопасности", target: "gov" },
    { text: "Осуществление мер по охране общественного порядка", target: "gov" },
    { text: "Осуществление мер по правам и свободам граждан", target: "gov" },
    { text: "Обеспечение единой государственной политики в области науки", target: "gov" },
    { text: "Представление Государственной Думе отчёта об исполнении федерального бюджета", target: "gov" },
    { text: "Регулирование в сфере внешнеэкономической деятельности", target: "gov" },
    { text: "Управление государственным долгом", target: "gov" },
    { text: "Обеспечение единого финансового, кредитного и денежного регулирования", target: "gov" },

    // --- Задача 1: +12 новых вопросов ---
    { text: "Назначение и освобождение Председателя Правительства в случае его отставки", target: "president" },
    { text: "Принятие решения об отставке Правительства РФ", target: "president" },
    { text: "Издание указов и распоряжений, обязательных для исполнения на всей территории РФ", target: "president" },
    { text: "Обеспечение осуществления полномочий федеральной государственной власти на всей территории РФ", target: "president" },
    { text: "Направление представителей Президента в федеральные округа", target: "president" },
    { text: "Назначение выборов Президента РФ в случае досрочного прекращения полномочий (в случае, предусмотренном Конституцией)", target: "sf" },
    { text: "Заслушивание ежегодных докладов Генерального прокурора РФ", target: "sf" },
    { text: "Лишение неприкосновенности экс-Президента РФ", target: "sf" },
    { text: "Назначение на должность и освобождение от должности членов Совета директоров Центрального банка (по представлению Председателя ЦБ)", target: "gd" },
    { text: "Заслушивание ежегодных отчётов Правительства о результатах деятельности", target: "gd" },
    { text: "Осуществление мер по обеспечению законности, прав и свобод граждан, охране собственности и общественного порядка, борьбе с преступностью", target: "gov" },
    { text: "Представление ежегодных докладов о результатах деятельности Правительства в Государственную Думу", target: "gov" }
];

const game = { score: 0, currentQuestion: null, isBusy: false };

function initGame() {
    safeAddListener('#gameBtn', 'click', () => {
        const hs = $('#highScore');
        if (hs) hs.textContent = localStorage.getItem(LS.HIGHSCORE) || 0;
        const start = $('#gameStartScreen');
        const play = $('#gamePlayScreen');
        if (start) start.hidden = false;
        if (play) play.hidden = true;
        const dlg = $('#gameDialog');
        if (dlg) dlg.showModal();
    });

    safeAddListener('#closeGame', 'click', () => $('#gameDialog').close());
    safeAddListener('#resetHighscoreBtn', 'click', () => {
        localStorage.removeItem(LS.HIGHSCORE);
        const hs = $('#highScore');
        if (hs) hs.textContent = 0;
        showToast('Рекорд сброшен');
    });
    safeAddListener('#startGameBtn', 'click', () => {
        game.score = 0;
        updateGameScore();
        $('#gameStartScreen').hidden = true;
        $('#gamePlayScreen').hidden = false;
        nextQuestion();
    });

    $$('.ans-btn').forEach(btn => btn.addEventListener('click', (e) => checkAnswer(e.target)));
}

function nextQuestion() {
    game.isBusy = false;
    const randomIndex = Math.floor(Math.random() * POWERS.length);
    game.currentQuestion = POWERS[randomIndex];
    const qText = $('#questionText');
    if (qText) {
        qText.style.opacity = 0;
        setTimeout(() => { qText.textContent = game.currentQuestion.text; qText.style.opacity = 1; }, 200);
    }
    $$('.ans-btn').forEach(btn => btn.className = 'ans-btn');
    const fb = $('#gameFeedback');
    if (fb) fb.textContent = "";
}

function checkAnswer(btn) {
    if (game.isBusy) return;
    game.isBusy = true;
    const target = btn.dataset.target;
    const isCorrect = target === game.currentQuestion.target;
    const fb = $('#gameFeedback');

    if (isCorrect) {
        btn.classList.add('correct');
        game.score++;
        if (fb) { fb.textContent = "Верно! 🎉"; fb.style.color = "#22c55e"; }
    } else {
        btn.classList.add('wrong');
        const correctBtn = $(`.ans-btn[data-target="${game.currentQuestion.target}"]`);
        if (correctBtn) correctBtn.classList.add('correct');
        if (fb) { fb.textContent = "Ошибка 😔"; fb.style.color = "#ef4444"; }
    }
    updateGameScore();
    const currentHigh = parseInt(localStorage.getItem(LS.HIGHSCORE) || 0);
    if (game.score > currentHigh) localStorage.setItem(LS.HIGHSCORE, game.score);
    setTimeout(nextQuestion, 1500);
}

function updateGameScore() {
    const sc = $('#currentScore');
    if (sc) sc.textContent = game.score;
}

/* --- ЗАДАНИЕ №23 (РАСШИРЕННАЯ БАЗА) --- */
const TASKS_23 = [
    {
        question: "РФ — социальное государство",
        options: [
            { id: 1, text: "Охрана труда и здоровья людей", correct: true },
            { id: 2, text: "Установление гарантированного МРОТ", correct: true },
            { id: 3, text: "Обеспечение государственной поддержки семьи, материнства, отцовства и детства", correct: true },
            { id: 4, text: "Развитие системы социальных служб", correct: true },
            { id: 5, text: "Установление государственных пенсий и пособий", correct: true },
            { id: 6, text: "Разделение государственной власти на три ветви", correct: false },
            { id: 7, text: "Признание идеологического многообразия", correct: false }
        ]
    },
    {
        question: "РФ — светское государство",
        options: [
            { id: 1, text: "Никакая религия не может устанавливаться в качестве государственной или обязательной", correct: true },
            { id: 2, text: "Религиозные объединения отделены от государства", correct: true },
            { id: 3, text: "Религиозные объединения равны перед законом", correct: true },
            { id: 4, text: "Гарантия свободы совести и вероисповедания", correct: true },
            { id: 5, text: "Каждый вправе исповедовать любую религию или не исповедовать никакой", correct: true },
            { id: 6, text: "Земля используется как основа жизни народов", correct: false },
            { id: 7, text: "Во взаимоотношениях с федеральными органами все субъекты равноправны", correct: false }
        ]
    },
    {
        question: "РФ — демократическое государство",
        options: [
            { id: 1, text: "Носителем суверенитета и единственным источником власти является народ", correct: true },
            { id: 2, text: "Народ осуществляет свою власть непосредственно, а также через органы власти", correct: true },
            { id: 3, text: "Высшим непосредственным выражением власти народа являются референдум и выборы", correct: true },
            { id: 4, text: "Признание политического многообразия и многопартийности", correct: true },
            { id: 5, text: "Местное самоуправление в пределах своих полномочий самостоятельно", correct: true },
            { id: 6, text: "Отделение церкви от государства", correct: false },
            { id: 7, text: "Обеспечение поддержки инвалидов и пожилых граждан", correct: false }
        ]
    },
    {
        question: "РФ — правовое государство",
        options: [
            { id: 1, text: "Верховенство закона и Конституции", correct: true },
            { id: 2, text: "Человек, его права и свободы являются высшей ценностью", correct: true },
            { id: 3, text: "Обязанность государства признавать, соблюдать и защищать права человека", correct: true },
            { id: 4, text: "Все равны перед законом и судом", correct: true },
            { id: 5, text: "Осуществление прав и свобод не должно нарушать права других лиц", correct: true },
            { id: 6, text: "Наличие единой денежной единицы", correct: false },
            { id: 7, text: "Установление государственных пенсий", correct: false }
        ]
    },
    {
        question: "РФ — федеративное государство",
        options: [
            { id: 1, text: "Состоит из республик, краев, областей, городов федерального значения", correct: true },
            { id: 2, text: "Равноправие субъектов РФ", correct: true },
            { id: 3, text: "Разграничение предметов ведения между РФ и субъектами", correct: true },
            { id: 4, text: "Государственная целостность", correct: true },
            { id: 5, text: "Единство системы государственной власти", correct: true },
            { id: 6, text: "Никакая религия не может быть государственной", correct: false },
            { id: 7, text: "Охрана труда и здоровья людей", correct: false }
        ]
    },
    {
        question: "РФ — республиканская форма правления",
        options: [
            { id: 1, text: "Глава государства (Президент) избирается сроком на 6 лет", correct: true },
            { id: 2, text: "Государственная Дума избирается сроком на 5 лет", correct: true },
            { id: 3, text: "Высшие органы власти являются выборными и сменяемыми", correct: true },
            { id: 4, text: "Главы субъектов Федерации избираются", correct: true },
            { id: 5, text: "Осуществление правосудия только судом", correct: false },
            { id: 6, text: "Признание идеологического многообразия", correct: false }
        ]
    },
    {
        question: "РФ — суверенное государство",
        options: [
            { id: 1, text: "Суверенитет РФ распространяется на всю её территорию", correct: true },
            { id: 2, text: "Конституция РФ и законы имеют верховенство на всей территории", correct: true },
            { id: 3, text: "РФ обеспечивает целостность и неприкосновенность своей территории", correct: true },
            { id: 4, text: "Запрет на отчуждение части территории РФ", correct: true },
            { id: 5, text: "Охрана труда и здоровья", correct: false },
            { id: 6, text: "Гарантия свободы совести", correct: false }
        ]
    },
    {
        question: "Единство экономического пространства",
        options: [
            { id: 1, text: "Свободное перемещение товаров, услуг и финансовых средств", correct: true },
            { id: 2, text: "Поддержка конкуренции", correct: true },
            { id: 3, text: "Свобода экономической деятельности", correct: true },
            { id: 4, text: "Признание и защита равным образом частной, государственной, муниципальной собственности", correct: true },
            { id: 5, text: "Запрет на установление таможенных границ внутри страны", correct: true },
            { id: 6, text: "Разделение властей на три ветви", correct: false },
            { id: 7, text: "Светский характер государства", correct: false }
        ]
    },
    {
        question: "Идеологическое многообразие",
        options: [
            { id: 1, text: "Никакая идеология не может устанавливаться в качестве государственной", correct: true },
            { id: 2, text: "Признание политического многообразия и многопартийности", correct: true },
            { id: 3, text: "Общественные объединения равны перед законом", correct: true },
            { id: 4, text: "Свобода мысли и слова", correct: true },
            { id: 5, text: "Запрет цензуры", correct: true },
            { id: 6, text: "Обеспечение государственной поддержки семьи", correct: false },
            { id: 7, text: "Единство экономического пространства", correct: false }
        ]
    },
    {
        question: "Гарантия презумпции невиновности",
        options: [
            { id: 1, text: "Каждый обвиняемый считается невиновным, пока его вина не будет доказана", correct: true },
            { id: 2, text: "Обвиняемый не обязан доказывать свою невиновность", correct: true },
            { id: 3, text: "Неустранимые сомнения в виновности толкуются в пользу обвиняемого", correct: true },
            { id: 4, text: "Никто не обязан свидетельствовать против самого себя", correct: true },
            { id: 5, text: "Свобода передвижения", correct: false },
            { id: 6, text: "Право на охрану здоровья", correct: false }
        ]
    },
    {
        question: "Гарантия личных (гражданских) прав",
        options: [
            { id: 1, text: "Право на жизнь", correct: true },
            { id: 2, text: "Достоинство личности охраняется государством", correct: true },
            { id: 3, text: "Право на свободу и личную неприкосновенность", correct: true },
            { id: 4, text: "Неприкосновенность жилища", correct: true },
            { id: 5, text: "Тайна переписки и телефонных переговоров", correct: true },
            { id: 6, text: "Право избирать и быть избранным", correct: false },
            { id: 7, text: "Право на участие в управлении делами государства", correct: false }
        ]
    },
    {
        question: "Гарантия политических прав",
        options: [
            { id: 1, text: "Право избирать и быть избранным", correct: true },
            { id: 2, text: "Право на участие в управлении делами государства", correct: true },
            { id: 3, text: "Равный доступ к государственной службе", correct: true },
            { id: 4, text: "Право собираться мирно, без оружия (митинги)", correct: true },
            { id: 5, text: "Право на объединение (создание профсоюзов, партий)", correct: true },
            { id: 6, text: "Право на охрану здоровья", correct: false },
            { id: 7, text: "Право на благоприятную окружающую среду", correct: false }
        ]
    },
    {
        question: "Гарантия социально-экономических прав",
        options: [
            { id: 1, text: "Свобода предпринимательской деятельности", correct: true },
            { id: 2, text: "Право частной собственности", correct: true },
            { id: 3, text: "Свобода труда", correct: true },
            { id: 4, text: "Право на отдых", correct: true },
            { id: 5, text: "Право на социальное обеспечение по возрасту", correct: true },
            { id: 6, text: "Право на жилище", correct: true },
            { id: 7, text: "Право на жизнь", correct: false }
        ]
    },
    {
        question: "Гарантия культурных прав",
        options: [
            { id: 1, text: "Свобода литературного, художественного и научного творчества", correct: true },
            { id: 2, text: "Право на участие в культурной жизни", correct: true },
            { id: 3, text: "Право на пользование учреждениями культуры", correct: true },
            { id: 4, text: "Доступ к культурным ценностям", correct: true },
            { id: 5, text: "Охрана интеллектуальной собственности", correct: true },
            { id: 6, text: "Право на судебную защиту", correct: false },
            { id: 7, text: "Неприкосновенность частной жизни", correct: false }
        ]
    },
    {
        question: "Обязанности гражданина РФ",
        options: [
            { id: 1, text: "Соблюдать Конституцию и законы", correct: true },
            { id: 2, text: "Платить законно установленные налоги и сборы", correct: true },
            { id: 3, text: "Сохранять природу и окружающую среду", correct: true },
            { id: 4, text: "Защищать Отечество", correct: true },
            { id: 5, text: "Заботиться о сохранении исторического и культурного наследия", correct: true },
            { id: 6, text: "Заботиться о детях и нетрудоспособных родителях", correct: true },
            { id: 7, text: "Участвовать в выборах", correct: false },
            { id: 8, text: "Вступать в политические партии", correct: false }
        ]
    },
    {
        question: "Полномочия Президента РФ",
        options: [
            { id: 1, text: "Введение военного положения", correct: true },
            { id: 2, text: "Осуществление помилования", correct: true },
            { id: 3, text: "Назначение выборов Государственной Думы", correct: true },
            { id: 4, text: "Руководство внешней политикой", correct: true },
            { id: 5, text: "Роспуск Государственной Думы в предусмотренных случаях", correct: true },
            { id: 6, text: "Объявление амнистии", correct: false },
            { id: 7, text: "Отрешение Президента от должности", correct: false }
        ]
    },
    {
        question: "Полномочия Совета Федерации",
        options: [
            { id: 1, text: "Утверждение изменения границ между субъектами РФ", correct: true },
            { id: 2, text: "Назначение судей Конституционного Суда", correct: true },
            { id: 3, text: "Назначение Генерального прокурора РФ", correct: true },
            { id: 4, text: "Отрешение Президента от должности", correct: true },
            { id: 5, text: "Утверждение указа о введении чрезвычайного положения", correct: true },
            { id: 6, text: "Объявление амнистии", correct: false },
            { id: 7, text: "Назначение Председателя Центрального банка", correct: false }
        ]
    },
    {
        question: "Полномочия Государственной Думы",
        options: [
            { id: 1, text: "Принятие федеральных законов", correct: true },
            { id: 2, text: "Объявление амнистии", correct: true },
            { id: 3, text: "Назначение Председателя Центрального банка", correct: true },
            { id: 4, text: "Выдвижение обвинения против Президента (импичмент)", correct: true },
            { id: 5, text: "Назначение Уполномоченного по правам человека", correct: true },
            { id: 6, text: "Введение военного положения", correct: false },
            { id: 7, text: "Назначение Генерального прокурора", correct: false }
        ]
    },
    {
        question: "Полномочия Правительства РФ",
        options: [
            { id: 1, text: "Разработка и представление федерального бюджета", correct: true },
            { id: 2, text: "Обеспечение единой финансовой политики", correct: true },
            { id: 3, text: "Управление федеральной собственностью", correct: true },
            { id: 4, text: "Осуществление мер по обеспечению обороны страны", correct: true },
            { id: 5, text: "Руководство деятельностью федеральных органов исполнительной власти", correct: true },
            { id: 6, text: "Введение чрезвычайного положения", correct: false },
            { id: 7, text: "Назначение выборов Президента", correct: false }
        ]
    },
    {
        question: "Предметы исключительного ведения Российской Федерации (ст. 71)",
        options: [
            { id: 1, text: "Принятие и изменение Конституции РФ", correct: true },
            { id: 2, text: "Федеральный бюджет, федеральные налоги и сборы", correct: true },
            { id: 3, text: "Внешняя политика и международные договоры", correct: true },
            { id: 4, text: "Оборона и безопасность", correct: true },
            { id: 5, text: "Уголовное и гражданское законодательство", correct: true },
            { id: 6, text: "Природопользование и охрана окружающей среды", correct: false },
            { id: 7, text: "Координация здравоохранения", correct: false }
        ]
    },
    {
        question: "Предметы совместного ведения РФ и субъектов (ст. 72)",
        options: [
            { id: 1, text: "Природопользование и охрана окружающей среды", correct: true },
            { id: 2, text: "Общие вопросы образования, воспитания и науки", correct: true },
            { id: 3, text: "Административное, трудовое, семейное, жилищное законодательство", correct: true },
            { id: 4, text: "Защита прав национальных меньшинств", correct: true },
            { id: 5, text: "Координация вопросов здравоохранения", correct: true },
            { id: 6, text: "Уголовное законодательство", correct: false },
            { id: 7, text: "Денежная эмиссия", correct: false }
        ]
    },
    {
        question: "Принципы правосудия в РФ (Глава 7)",
        options: [
            { id: 1, text: "Осуществление правосудия только судом", correct: true },
            { id: 2, text: "Независимость судей", correct: true },
            { id: 3, text: "Состязательность и равноправие сторон", correct: true },
            { id: 4, text: "Презумпция невиновности", correct: true },
            { id: 5, text: "Гласность судопроизводства", correct: true },
            { id: 6, text: "Обязательное участие прокурора в любом процессе", correct: false },
            { id: 7, text: "Назначение наказания без судебного решения", correct: false }
        ]
    },
    {
        question: "Личные (гражданские) права и свободы человека",
        options: [
            { id: 1, text: "Право на жизнь", correct: true },
            { id: 2, text: "Право на неприкосновенность частной жизни", correct: true },
            { id: 3, text: "Свобода передвижения и выбора места жительства", correct: true },
            { id: 4, text: "Право на тайну переписки и телефонных переговоров", correct: true },
            { id: 5, text: "Неприкосновенность жилища", correct: true },
            { id: 6, text: "Право избирать и быть избранным", correct: false },
            { id: 7, text: "Право на труд и отдых", correct: false }
        ]
    },
    {
        question: "Социальные права граждан РФ",
        options: [
            { id: 1, text: "Право на труд и защиту от безработицы", correct: true },
            { id: 2, text: "Право на социальное обеспечение по возрасту и болезни", correct: true },
            { id: 3, text: "Право на жилище", correct: true },
            { id: 4, text: "Право на охрану здоровья и медицинскую помощь", correct: true },
            { id: 5, text: "Право на образование", correct: true },
            { id: 6, text: "Право на участие в управлении делами государства", correct: false },
            { id: 7, text: "Свобода слова и мысли", correct: false }
        ]
    },
    {
        question: "Характеристики Конституции РФ как Основного закона",
        options: [
            { id: 1, text: "Обладает высшей юридической силой", correct: true },
            { id: 2, text: "Имеет прямое действие на всей территории РФ", correct: true },
            { id: 3, text: "Законы не должны противоречить Конституции", correct: true },
            { id: 4, text: "Органы власти обязаны соблюдать Конституцию", correct: true },
            { id: 5, text: "Принимается на референдуме или Конституционным Собранием", correct: true },
            { id: 6, text: "Может быть изменена простым большинством голосов в ГД", correct: false },
            { id: 7, text: "Вступает в силу только после одобрения Президентом", correct: false }
        ]
    },
    {
        question: "Принципы федеративного устройства РФ (ст. 5)",
        options: [
            { id: 1, text: "Государственная целостность", correct: true },
            { id: 2, text: "Единство системы государственной власти", correct: true },
            { id: 3, text: "Равноправие субъектов РФ", correct: true },
            { id: 4, text: "Разграничение предметов ведения между РФ и субъектами", correct: true },
            { id: 5, text: "Самоопределение народов в РФ", correct: true },
            { id: 6, text: "Право субъектов на выход из состава РФ", correct: false },
            { id: 7, text: "Установление субъектами собственной валюты", correct: false }
        ]
    },
    {
        question: "Народный суверенитет и формы его осуществления (ст. 3)",
        options: [
            { id: 1, text: "Народ является единственным источником власти", correct: true },
            { id: 2, text: "Народ осуществляет власть непосредственно через референдум и выборы", correct: true },
            { id: 3, text: "Народ осуществляет власть через органы государственной власти", correct: true },
            { id: 4, text: "Народ осуществляет власть через органы местного самоуправления", correct: true },
            { id: 5, text: "Захват власти или присвоение властных полномочий преследуется по закону", correct: true },
            { id: 6, text: "Правительство является источником власти", correct: false },
            { id: 7, text: "Власть Президента не зависит от воли народа", correct: false }
        ]
    },
    {
        question: "Принципы правового государства",
        options: [
            { id: 1, text: "Верховенство Конституции и закона", correct: true },
            { id: 2, text: "Права и свободы человека — высшая ценность", correct: true },
            { id: 3, text: "Разделение властей на законодательную, исполнительную и судебную", correct: true },
            { id: 4, text: "Взаимная ответственность государства и личности", correct: true },
            { id: 5, text: "Гарантии судебной защиты прав граждан", correct: true },
            { id: 6, text: "Государство не связано правом", correct: false },
            { id: 7, text: "Исполнительная власть выше судебной", correct: false }
        ]
    },
    {
        question: "Гарантии неприкосновенности личности (ст. 22—25)",
        options: [
            { id: 1, text: "Арест только по судебному решению", correct: true },
            { id: 2, text: "Задержание без решения суда — не более 48 часов", correct: true },
            { id: 3, text: "Неприкосновенность жилища без судебного решения", correct: true },
            { id: 4, text: "Тайна переписки, телефонных переговоров", correct: true },
            { id: 5, text: "Защита персональных данных", correct: true },
            { id: 6, text: "Заключение под стражу по решению прокурора", correct: false },
            { id: 7, text: "Обыск без ограничений", correct: false }
        ]
    },
    {
        question: "Права в сфере правосудия и юридической защиты",
        options: [
            { id: 1, text: "Право на судебную защиту", correct: true },
            { id: 2, text: "Право на квалифицированную юридическую помощь (адвокат)", correct: true },
            { id: 3, text: "Презумпция невиновности", correct: true },
            { id: 4, text: "Право на рассмотрение дела судом присяжных (в предусмотренных случаях)", correct: true },
            { id: 5, text: "Запрет использования доказательств, полученных с нарушением закона", correct: true },
            { id: 6, text: "Право требовать изменить состав суда по желанию обвиняемого", correct: false },
            { id: 7, text: "Обязанность обвиняемого доказывать свою невиновность", correct: false }
        ]
    },
    {
        question: "Виды субъектов РФ (ст. 5, 65)",
        options: [
            { id: 1, text: "Республики", correct: true },
            { id: 2, text: "Края", correct: true },
            { id: 3, text: "Области", correct: true },
            { id: 4, text: "Города федерального значения", correct: true },
            { id: 5, text: "Автономная область и автономные округа", correct: true },
            { id: 6, text: "Федеральные районы", correct: false },
            { id: 7, text: "Губернии", correct: false }
        ]
    },

    // --- Задача 2: +9 новых заданий ---
    {
        question: "Права, которые не могут быть ограничены даже в условиях чрезвычайного положения (ст. 56)",
        options: [
            { id: 1, text: "Право на жизнь", correct: true },
            { id: 2, text: "Достоинство личности", correct: true },
            { id: 3, text: "Право не подвергаться пыткам", correct: true },
            { id: 4, text: "Право на неприкосновенность частной жизни", correct: true },
            { id: 5, text: "Свобода совести и вероисповедания", correct: true },
            { id: 6, text: "Право на забастовку", correct: false },
            { id: 7, text: "Свобода передвижения", correct: false }
        ]
    },
    {
        question: "Основания для роспуска Государственной Думы Президентом (ст. 111, 117)",
        options: [
            { id: 1, text: "Трёхкратное отклонение кандидатур Председателя Правительства", correct: true },
            { id: 2, text: "Повторное выражение недоверия Правительству в течение трёх месяцев", correct: true },
            { id: 3, text: "Выражение недоверия Правительству по инициативе самого Правительства (отказ в доверии)", correct: true },
            { id: 4, text: "Нарушение Думой Конституции по решению Конституционного Суда", correct: false },
            { id: 5, text: "Требование Совета Федерации", correct: false }
        ]
    },
    {
        question: "Случаи, когда Государственная Дума не может быть распущена (ст. 109)",
        options: [
            { id: 1, text: "В течение года после её избрания", correct: true },
            { id: 2, text: "С момента выдвижения обвинения против Президента до принятия решения Советом Федерации", correct: true },
            { id: 3, text: "В период действия военного положения", correct: true },
            { id: 4, text: "В период действия чрезвычайного положения", correct: true },
            { id: 5, text: "В течение шести месяцев до окончания срока полномочий Президента", correct: true },
            { id: 6, text: "Если Дума приняла бюджет", correct: false },
            { id: 7, text: "Во время сессии", correct: false }
        ]
    },
    {
        question: "Требования к кандидату в Президенты РФ (ст. 81)",
        options: [
            { id: 1, text: "Гражданин РФ", correct: true },
            { id: 2, text: "Не моложе 35 лет", correct: true },
            { id: 3, text: "Постоянно проживает в РФ не менее 25 лет", correct: true },
            { id: 4, text: "Не имеет иностранного гражданства или вида на жительство", correct: true },
            { id: 5, text: "Не имеет счетов и вкладов в иностранных банках", correct: true },
            { id: 6, text: "Имеет высшее юридическое образование", correct: false },
            { id: 7, text: "Не моложе 40 лет", correct: false }
        ]
    },
    {
        question: "Основания прекращения полномочий Президента РФ досрочно (ст. 92, 93)",
        options: [
            { id: 1, text: "Отставка по собственному желанию", correct: true },
            { id: 2, text: "Стойкая неспособность по состоянию здоровья осуществлять полномочия", correct: true },
            { id: 3, text: "Отрешение от должности Советом Федерации", correct: true },
            { id: 4, text: "Истечение срока полномочий", correct: false },
            { id: 5, text: "Вотум недоверия Государственной Думы", correct: false }
        ]
    },
    {
        question: "Принципы гражданства Российской Федерации (ст. 6)",
        options: [
            { id: 1, text: "Гражданство является единым", correct: true },
            { id: 2, text: "Гражданство является равным независимо от оснований приобретения", correct: true },
            { id: 3, text: "Гражданин не может быть лишён гражданства", correct: true },
            { id: 4, text: "Гражданин не может быть выслан за пределы РФ", correct: true },
            { id: 5, text: "Гражданин не может быть выдан иностранному государству", correct: true },
            { id: 6, text: "Двойное гражданство запрещено во всех случаях", correct: false },
            { id: 7, text: "Гражданство субъекта РФ не совпадает с гражданством РФ", correct: false }
        ]
    },
    {
        question: "Полномочия Конституционного Суда РФ (ст. 125)",
        options: [
            { id: 1, text: "Проверка конституционности федеральных законов", correct: true },
            { id: 2, text: "Разрешение споров о компетенции между федеральными органами власти", correct: true },
            { id: 3, text: "Толкование Конституции РФ", correct: true },
            { id: 4, text: "Проверка конституционности закона по жалобам граждан", correct: true },
            { id: 5, text: "Дача заключения о соблюдении порядка выдвижения обвинения против Президента", correct: true },
            { id: 6, text: "Рассмотрение уголовных дел по первой инстанции", correct: false },
            { id: 7, text: "Назначение Генерального прокурора", correct: false }
        ]
    },
    {
        question: "Конституционные гарантии права на труд (ст. 37)",
        options: [
            { id: 1, text: "Труд свободен, каждый вправе свободно выбирать род деятельности", correct: true },
            { id: 2, text: "Принудительный труд запрещён", correct: true },
            { id: 3, text: "Право на безопасные условия труда", correct: true },
            { id: 4, text: "Право на вознаграждение за труд без дискриминации не ниже МРОТ", correct: true },
            { id: 5, text: "Право на защиту от безработицы", correct: true },
            { id: 6, text: "Государство гарантирует каждому место работы", correct: false },
            { id: 7, text: "Работодатель обязан принимать на работу всех желающих", correct: false }
        ]
    },
    {
        question: "Конституционные принципы судебной системы (ст. 118—123)",
        options: [
            { id: 1, text: "Правосудие осуществляется только судом", correct: true },
            { id: 2, text: "Судьи независимы и подчиняются только Конституции и закону", correct: true },
            { id: 3, text: "Судьи несменяемы", correct: true },
            { id: 4, text: "Судьи неприкосновенны", correct: true },
            { id: 5, text: "Разбирательство дел во всех судах открытое", correct: true },
            { id: 6, text: "Судья может быть одновременно прокурором", correct: false },
            { id: 7, text: "Президент вправе давать указания судьям по конкретным делам", correct: false }
        ]
    }
];

const game23 = { currentTaskIndex: 0, selectedIds: new Set(), initialized: false };

function initGame23() {
    safeAddListener('#game23Btn', 'click', () => {
        // Инициализировать только при первом открытии
        if (!game23.initialized) {
            TASKS_23.sort(() => Math.random() - 0.5);
            game23.initialized = true;
        }
        renderTask23();
        $('#game23Dialog').showModal();
    });
    safeAddListener('#closeGame23', 'click', () => $('#game23Dialog').close());

    safeAddListener('#resetGame23', 'click', () => {
        game23.currentTaskIndex = 0;
        game23.selectedIds = new Set();
        TASKS_23.sort(() => Math.random() - 0.5);
        renderTask23();
        $('#task23Feedback').textContent = '';
    });

    safeAddListener('#checkTask23Btn', 'click', checkTask23);
    safeAddListener('#nextTask23Btn', 'click', () => {
        game23.currentTaskIndex = (game23.currentTaskIndex + 1) % TASKS_23.length;
        renderTask23();
    });
}

function renderTask23() {
    const task = TASKS_23[game23.currentTaskIndex];
    game23.selectedIds.clear();

    $('#task23Question').textContent = task.question;
    const container = $('#task23Options');
    container.innerHTML = '';
    // Перемешиваем варианты ответов
    const shuffled = [...task.options].sort(() => Math.random() - 0.5);

    shuffled.forEach(opt => {
        const div = document.createElement('div');
        div.className = 'task23-option';
        div.textContent = opt.text;
        div.dataset.id = opt.id;
        div.addEventListener('click', () => toggleOption23(div, opt.id));
        container.appendChild(div);
    });

    $('#checkTask23Btn').disabled = true;
    $('#checkTask23Btn').style.display = 'inline-block';
    $('#nextTask23Btn').style.display = 'none';
    $('#task23Feedback').textContent = '';
}

function toggleOption23(el, id) {
    if ($('#nextTask23Btn').style.display === 'inline-block') return;

    if (game23.selectedIds.has(id)) {
        game23.selectedIds.delete(id);
        el.classList.remove('selected');
    } else {
        if (game23.selectedIds.size < 3) {
            game23.selectedIds.add(id);
            el.classList.add('selected');
        }
    }
    $('#checkTask23Btn').disabled = game23.selectedIds.size !== 3;
}

function checkTask23() {
    const task = TASKS_23[game23.currentTaskIndex];
    const correctIds = new Set(task.options.filter(o => o.correct).map(o => o.id));
    let errors = 0;

    $$('.task23-option').forEach(el => {
        const id = parseInt(el.dataset.id);
        const isSelected = game23.selectedIds.has(id);
        const isCorrect = correctIds.has(id);

        if (isSelected && isCorrect) {
            el.classList.add('correct');
        } else if (isSelected && !isCorrect) {
            el.classList.add('wrong');
            errors++;
        } else if (!isSelected && isCorrect) {
            el.style.border = "2px dashed #22c55e";
        }
    });

    const fb = $('#task23Feedback');
    if (errors === 0 && game23.selectedIds.size === 3) {
        fb.textContent = "Отлично! Все верно. +3 балла";
        fb.style.color = "#22c55e";
    } else {
        fb.textContent = `Ошибок: ${errors}. Попробуйте запомнить верные положения.`;
        fb.style.color = "#ef4444";
    }

    $('#checkTask23Btn').style.display = 'none';
    $('#nextTask23Btn').style.display = 'inline-block';
}

/* --- FLASHCARDS --- */
const flashcards = {
    terms: [],
    index: 0
};

function initFlashcards() {
    safeAddListener('#flashcardsBtn', 'click', () => {
        // Инициализировать только если пусто (первое открытие)
        if (flashcards.terms.length === 0) {
            flashcards.terms = Object.keys(DICTIONARY).sort(() => Math.random() - 0.5);
            flashcards.index = 0;
        }
        renderFlashcard();
        $('#flashcardsDialog').showModal();
    });

    safeAddListener('#closeFlashcards', 'click', () => $('#flashcardsDialog').close());

    safeAddListener('#resetFlashcards', 'click', () => {
        flashcards.terms = Object.keys(DICTIONARY).sort(() => Math.random() - 0.5);
        flashcards.index = 0;
        renderFlashcard();
    });

    safeAddListener('#fcNext', 'click', () => {
        if (flashcards.index < flashcards.terms.length - 1) {
            $('#flashcard').classList.remove('flipped');
            setTimeout(() => {
                flashcards.index++;
                renderFlashcard();
            }, 300);
        }
    });

    safeAddListener('#fcPrev', 'click', () => {
        if (flashcards.index > 0) {
            $('#flashcard').classList.remove('flipped');
            setTimeout(() => {
                flashcards.index--;
                renderFlashcard();
            }, 300);
        }
    });

    safeAddListener('#flashcard', 'click', () => {
        $('#flashcard').classList.toggle('flipped');
    });
}

function renderFlashcard() {
    if (flashcards.terms.length === 0) return;
    const term = flashcards.terms[flashcards.index];
    
    $('#fcTerm').textContent = term.charAt(0).toUpperCase() + term.slice(1);
    $('#fcDef').textContent = DICTIONARY[term];
    $('#fcCounter').textContent = `${flashcards.index + 1} / ${flashcards.terms.length}`;

    $('#fcPrev').disabled = flashcards.index === 0;
    $('#fcNext').disabled = flashcards.index === flashcards.terms.length - 1;
}

/* --- ШРИФТЫ --- */
function initFontSettings() {
    const saved = JSON.parse(localStorage.getItem(LS.FONT));
    if (saved) {
        state.fontSize = saved.size;
        state.lineHeight = saved.height;
    }
    const savedType = localStorage.getItem(LS.FONT_TYPE);
    if (savedType) {
        if (savedType === 'serif') document.body.classList.add('serif-mode');
        const rb = $(`input[name="fontType"][value="${savedType}"]`);
        if (rb) rb.checked = true;
    }

    applyFontSettings();

    safeAddListener('#fontBtn', 'click', () => {
        const dlg = $('#fontSettingsDialog');
        if (dlg) dlg.open ? dlg.close() : dlg.showModal();
    });

    safeAddListener('#fontInc', 'click', () => changeFont(1));
    safeAddListener('#fontDec', 'click', () => changeFont(-1));
    safeAddListener('#lhInc', 'click', () => changeLH(0.1));
    safeAddListener('#lhDec', 'click', () => changeLH(-0.1));

    // Font Type Toggle
    $$('input[name="fontType"]').forEach(rb => {
        rb.addEventListener('change', (e) => {
            const val = e.target.value;
            if (val === 'serif') document.body.classList.add('serif-mode');
            else document.body.classList.remove('serif-mode');
            localStorage.setItem(LS.FONT_TYPE, val);
        });
    });
}

function changeFont(delta) {
    state.fontSize = Math.max(12, Math.min(24, state.fontSize + delta));
    applyFontSettings();
}

function changeLH(delta) {
    state.lineHeight = Math.max(1.2, Math.min(2.0, parseFloat((state.lineHeight + delta).toFixed(1))));
    applyFontSettings();
}

function applyFontSettings() {
    document.documentElement.style.setProperty('--font-size', state.fontSize + 'px');
    document.documentElement.style.setProperty('--line-height', state.lineHeight);
    localStorage.setItem(LS.FONT, JSON.stringify({ size: state.fontSize, height: state.lineHeight }));
    const sizeVal = $('#fontSizeVal');
    const lhVal = $('#lhVal');
    if (sizeVal) sizeVal.textContent = state.fontSize + 'px';
    if (lhVal) lhVal.textContent = state.lineHeight.toFixed(1);
}

/* --- ТАЙМЕР --- */
function initTimer() {
    const timerEl = $('#egeTimer');
    if (!timerEl) return;
    const examDate = new Date('2026-06-11T09:00:00');

    function update() {
        const now = new Date();
        const diff = examDate - now;
        if (diff <= 0) { timerEl.innerHTML = "ЕГЭ уже идет!"; return; }
        const days = Math.floor(diff / (1000 * 60 * 60 * 24));
        let txt = 'дней';
        const lastDigit = days % 10;
        const lastTwo = days % 100;
        if (lastDigit === 1 && lastTwo !== 11) txt = 'день';
        else if ([2, 3, 4].includes(lastDigit) && ![12, 13, 14].includes(lastTwo)) txt = 'дня';
        timerEl.innerHTML = `До ЕГЭ по обществознанию:<br><span>${days} ${txt}</span>`;
    }
    update();
    setInterval(update, 1000 * 60 * 60);
}

/* --- ПОИСК --- */
function initSearchHistory() {
    const stored = localStorage.getItem(LS.SEARCH);
    if (stored) state.searchHistory = JSON.parse(stored);

    const input = $('#searchInput');
    const container = $('#searchHistory');
    if (!input || !container) return;

    input.addEventListener('focus', () => {
        if (state.searchHistory.length > 0 && input.value === '') {
            renderSearchHistory();
            container.hidden = false;
        }
    });

    const debouncedSearch = debounce((q) => {
        if (!q && state.searchHistory.length > 0) {
            renderSearchHistory();
            container.hidden = false;
        } else {
            container.hidden = true;
            filterArticles(q);
        }
    }, 300);

    input.addEventListener('input', (e) => {
        debouncedSearch(e.target.value);
    });

    document.addEventListener('click', (e) => {
        if (!e.target.closest('.search-container')) container.hidden = true;
    });
}

function saveSearchQuery(query) {
    if (!query) return;
    state.searchHistory = state.searchHistory.filter(q => q !== query);
    state.searchHistory.unshift(query);
    if (state.searchHistory.length > 5) state.searchHistory.pop();
    localStorage.setItem(LS.SEARCH, JSON.stringify(state.searchHistory));
}

function renderSearchHistory() {
    const container = $('#searchHistory');
    if (!container) return;
    container.innerHTML = '';
    state.searchHistory.forEach(q => {
        const item = document.createElement('div');
        item.className = 'search-history-item';
        item.textContent = q;
        item.addEventListener('click', () => {
            $('#searchInput').value = q;
            container.hidden = true;
            performSearch(q);
        });
        container.appendChild(item);
    });
}

function performSearch(query) {
    saveSearchQuery(query);
    filterArticles(query);
    $('#searchHistory').hidden = true;
}

function levenshtein(a, b) {
    if (a.length === 0) return b.length;
    if (b.length === 0) return a.length;
    const matrix = [];
    for (let i = 0; i <= b.length; i++) matrix[i] = [i];
    for (let j = 0; j <= a.length; j++) matrix[0][j] = j;
    for (let i = 1; i <= b.length; i++) {
        for (let j = 1; j <= a.length; j++) {
            if (b.charAt(i - 1) === a.charAt(j - 1)) {
                matrix[i][j] = matrix[i - 1][j - 1];
            } else {
                matrix[i][j] = Math.min(matrix[i - 1][j - 1] + 1, matrix[i][j - 1] + 1, matrix[i - 1][j] + 1);
            }
        }
    }
    return matrix[b.length][a.length];
}

function filterArticles(query) {
    query = query.trim().toLowerCase();
    state.activeSearchQuery = query;

    if (!query) { renderArticles(state.articles); return; }

    const sourceList = state.showFavoritesOnly ? state.articles.filter(a => state.favorites.has(a.id)) : state.articles;

    // Fuzzy Filter Logic
    const filtered = sourceList.filter(a => {
        const t = a.title.toLowerCase();
        const body = a.bodyHTML.replace(/<[^>]+>/g, ' ').toLowerCase();

        if (t.includes(query) || body.includes(query)) return true;

        if (query.length > 3) {
            const titleWords = t.split(/\s+/);
            const bodyWords = body.split(/\s+/);

            const matchWord = (word) => {
                if (Math.abs(word.length - query.length) > 2) return false;
                const dist = levenshtein(word, query);
                return dist <= 2;
            };

            return titleWords.some(matchWord) || bodyWords.some(matchWord);
        }
        return false;
    });

    renderArticles(filtered);
}

function processText(text) {
    const articleRegex = /(стать(?:ей|ями|е|ю|я|и)\s+)((?:[\d\.\,\s–-]+|(?:\([^\)]+\))|и)+)/gi;
    text = text.replace(articleRegex, (match, prefix, listContent) => {
        const linkedList = listContent.replace(/((?:часть|пункт)\s+)?(\d+(?:\.\d+)?)/gi, (m, keyword, num) => {
            if (keyword) return m;
            const targetArt = state.articles.find(a => a.title.startsWith(`Статья ${num}`));
            if (targetArt) return `<a href="#${targetArt.id}" class="cross-link" data-target="${targetArt.id}">${num}</a>`;
            return num;
        });
        return prefix + linkedList;
    });

    return text;
}

/* --- FAV FOLDERS LOGIC --- */
function loadFavorites() {
    const stored = localStorage.getItem(LS.FAVORITES);
    if (stored) { state.favorites = new Set(JSON.parse(stored)); }
    
    const storedFolders = localStorage.getItem(LS.FAV_FOLDERS);
    if (storedFolders) { state.favFolders = JSON.parse(storedFolders); }

    const storedMap = localStorage.getItem(LS.ARTICLE_FOLDERS);
    if (storedMap) { state.articleFolders = JSON.parse(storedMap); }

    updateFavCount();
}

function toggleFavorite(id) {
    if (state.favorites.has(id)) {
        state.favorites.delete(id);
        delete state.articleFolders[id];
        saveFolders();
    } else {
        state.favorites.add(id);
        state.articleFolders[id] = 'General'; // Default
        saveFolders();
    }
    localStorage.setItem(LS.FAVORITES, JSON.stringify([...state.favorites]));
    updateFavCount();
    renderArticles();
}

function saveFolders() {
    localStorage.setItem(LS.FAV_FOLDERS, JSON.stringify(state.favFolders));
    localStorage.setItem(LS.ARTICLE_FOLDERS, JSON.stringify(state.articleFolders));
}

function updateFavCount() {
    const badge = $('#favCount');
    if (badge) badge.textContent = state.favorites.size;
}

function setFavFilterMode() {
    state.showFavoritesOnly = !state.showFavoritesOnly;
    const btn = $('#favFilterBtn');

    if (state.showFavoritesOnly) {
        btn.setAttribute('aria-pressed', 'true');
        btn.innerHTML = `⭐ Скрыть избранное <span class="badge">${state.favorites.size}</span>`;
    } else {
        btn.setAttribute('aria-pressed', 'false');
        btn.innerHTML = `⭐ Избранное <span class="badge">${state.favorites.size}</span>`;
        state.currentFolderFilter = 'all'; // reset
    }

    const search = $('#searchInput');
    if (search) search.value = '';
    state.activeSearchQuery = '';
    renderArticles();
}

function renderFolderSelect() {
    const select = $('#folderSelectFilter');
    select.innerHTML = '<option value="all">Все папки</option>';
    state.favFolders.forEach(f => {
        const opt = document.createElement('option');
        opt.value = f;
        opt.textContent = f;
        if (f === state.currentFolderFilter) opt.selected = true;
        select.appendChild(opt);
    });
}

/* --- ЗАМЕТКИ --- */
function loadNotes() {
    const stored = localStorage.getItem(LS.NOTES);
    if (stored) state.notes = JSON.parse(stored);
}

function saveNote(id, text) {
    if (!text.trim()) delete state.notes[id];
    else state.notes[id] = text;
    localStorage.setItem(LS.NOTES, JSON.stringify(state.notes));
}

function applyTheme(init = false) {
    let t = localStorage.getItem(LS.THEME);
    if (!t && init) {
        const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        t = systemPrefersDark ? 'dark' : 'light';
    } else if (!t) { t = 'dark'; }
    document.documentElement.classList.toggle('light', t === 'light');
    if (!init) updateScrollState();
}

function toggleTheme() {
    const isLight = document.documentElement.classList.contains('light');
    const newTheme = isLight ? 'dark' : 'light';
    localStorage.setItem(LS.THEME, newTheme);
    applyTheme();
}

/* --- SCROLL & SPY LOGIC --- */
function updateScrollState() {
    const scrollTop = window.scrollY;
    const bar = $('#scrollProgress .bar');
    if (bar) {
        const docHeight = document.body.scrollHeight;
        const winHeight = window.innerHeight;
        const scrollPercent = scrollTop / (docHeight - winHeight);
        bar.style.width = Math.round(scrollPercent * 100) + '%';
    }
    const btnUp = $('#backToTop');
    if (btnUp) {
        if (scrollTop > 300) btnUp.classList.add('visible');
        else btnUp.classList.remove('visible');
    }
}

function initSpyScroll() {
    const toc = $('#toc');
    const checkActiveChapter = debounce(() => {
        if (!toc) return;
        const cards = $$('.card');
        if (cards.length === 0) return;

        const headerOffset = 100;
        let activeCard = null;
        
        for (let card of cards) {
            const rect = card.getBoundingClientRect();
            if (rect.bottom > headerOffset) {
                activeCard = card;
                break;
            }
        }

        if (!activeCard) return;

        const articleId = activeCard.dataset.articleId;
        const article = state.articles.find(a => a.id === articleId);

        if (article) {
            $$('.toc-chapter').forEach(el => el.classList.remove('active'));
            const chapters = $$('.toc-chapter');
            chapters.forEach(ch => {
                const titleSpan = ch.querySelector('.toc-chapter-header span:first-child');
                if (titleSpan && titleSpan.textContent === article.chapterTitle) {
                    ch.classList.add('active');
                }
            });
        }
    }, 100);

    window.addEventListener('scroll', checkActiveChapter);
}

function scrollToTop() { window.scrollTo({ top: 0, behavior: 'smooth' }); }
function showReturnButton() { const btn = $('#btn-return'); if (btn) btn.classList.add('visible'); }
function hideReturnButton() { const btn = $('#btn-return'); if (btn) btn.classList.remove('visible'); }

function returnBack() {
    if (state.returnPosition !== null) {
        state.isJumping = true;
        window.scrollTo({ top: state.returnPosition, behavior: 'smooth' });
        hideReturnButton();
        setTimeout(() => {
            state.isJumping = false;
            state.returnPosition = null;
            state.landingPosition = null;
        }, 1000);
    }
}

function buildTOC() {
    const nav = $('#toc');
    if (!nav) return;
    nav.innerHTML = '<div class="toc-title">Оглавление</div><ul class="toc-list"></ul>';
    const ul = $('.toc-list', nav);

    const chapters = {};
    state.articles.forEach(a => {
        if (!chapters[a.chapterTitle]) chapters[a.chapterTitle] = [];
        chapters[a.chapterTitle].push(a);
    });

    Object.keys(chapters).forEach(chTitle => {
        const li = document.createElement('li');
        li.className = 'toc-chapter';
        const header = document.createElement('div');
        header.className = 'toc-chapter-header';
        header.innerHTML = `<span>${chTitle}</span><span class="toc-toggle-icon">▼</span>`;
        header.addEventListener('click', () => { li.classList.toggle('open'); });

        const subUl = document.createElement('ul');
        subUl.className = 'toc-articles';
        chapters[chTitle].forEach(art => {
            const subLi = document.createElement('li');
            const a = document.createElement('a');
            a.href = '#';
            a.textContent = art.title;
            a.addEventListener('click', e => {
                e.preventDefault();
                // Закрываем мобильное боковое меню сразу
                const sidebarPanel = $('#sidebarPanel');
                if (sidebarPanel) sidebarPanel.classList.remove('visible');
                $$('.nav-item').forEach(b => b.classList.remove('active'));
                if (state.showFavoritesOnly) setFavFilterMode();
                const el = document.getElementById(art.id);
                if (el) {
                    const offset = 80;
                    const bodyRect = document.body.getBoundingClientRect().top;
                    const elementRect = el.getBoundingClientRect().top;
                    const elementPosition = elementRect - bodyRect;
                    window.scrollTo({ top: elementPosition - offset, behavior: "smooth" });
                }
            });
            subLi.append(a);
            subUl.append(subLi);
        });
        li.append(header);
        li.append(subUl);
        ul.append(li);
    });
}

function renderArticles(list = state.articles) {
    const container = $('#content');
    if (!container) return;
    container.innerHTML = '';

    let displayList = list;
    if (state.showFavoritesOnly) {
        displayList = list.filter(a => state.favorites.has(a.id));
        // Filter by Folder
        if (state.currentFolderFilter !== 'all') {
            displayList = displayList.filter(a => state.articleFolders[a.id] === state.currentFolderFilter);
        }

        if (displayList.length === 0) {
            container.innerHTML = '<div style="padding:20px; text-align:center; color:var(--muted)">В этой папке пока ничего нет.</div>';
            return;
        }
    }

    if (displayList.length === 0 && state.activeSearchQuery) {
        container.innerHTML = '<div style="padding:20px; text-align:center; color:var(--muted)">Ничего не найдено.</div>';
        return;
    }

    const template = $('#articleCardTmpl');

    displayList.forEach(a => {
        const node = template.content.cloneNode(true);
        const card = $('.card', node);
        card.dataset.articleId = a.id;
        card.id = a.id;

        const crumbs = $('.breadcrumbs', node);
        const chShort = a.chapterTitle.split('.')[0] || a.chapterTitle;
        crumbs.textContent = `${chShort.trim()} > ${a.title}`;

        $('.title', node).textContent = a.title;

        let processedBody = processText(a.bodyHTML);
        if (state.activeSearchQuery && state.activeSearchQuery.length > 2) {
             const escaped = state.activeSearchQuery.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
             const re = new RegExp(`(${escaped})(?![^<]*>)`, 'gi');
             processedBody = processedBody.replace(re, '<mark>$1</mark>');
        }
        $('.body', node).innerHTML = processedBody;

        const explain = $('.explain', node);
        let processedExplain = a.explainHTML ? processText(a.explainHTML) : '';
        let foundInExplain = false;

        if (a.explainHTML) {
             if (state.activeSearchQuery && state.activeSearchQuery.length > 2) {
                const escaped = state.activeSearchQuery.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
                const re = new RegExp(`(${escaped})(?![^<]*>)`, 'gi');
                if (re.test(processedExplain)) foundInExplain = true;
                processedExplain = processedExplain.replace(re, '<mark>$1</mark>');
            }
            $('.explain-body', node).innerHTML = processedExplain;
            
            // ALWAYS VISIBLE (removed TeacherMode check)
            if (foundInExplain) { explain.open = true; }
        } else { explain.hidden = true; }

        const favBtn = $('.btn-fav', node);
        const isFav = state.favorites.has(a.id);
        favBtn.textContent = isFav ? '★' : '☆';
        if (isFav) favBtn.classList.add('active');
        favBtn.addEventListener('click', (e) => { e.stopPropagation(); toggleFavorite(a.id); });


        const shareBtn = $('.btn-share', node);
        shareBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            openShareDialog(a.title, a.bodyHTML.replace(/<[^>]+>/g, ' '));
        });

        const link = $('.deeplink', node);
        link.href = `#${a.id}`;
        link.addEventListener('click', e => {
            e.preventDefault(); e.stopPropagation();
            history.replaceState(null, '', `#${a.id}`);
            navigator.clipboard.writeText(window.location.href).then(showToast);
        });

        const noteBtn = $('.btn-note', node);
        const noteContainer = $('.note-container', node);
        const noteArea = $('.note-area', node);
        
        if (state.notes[a.id]) {
            noteArea.value = state.notes[a.id];
            noteContainer.hidden = false;
            noteBtn.classList.add('active');
        }

        noteBtn.addEventListener('click', (e) => {
            e.stopPropagation();
            noteContainer.hidden = !noteContainer.hidden;
            if (!noteContainer.hidden) noteArea.focus();
        });

        noteArea.addEventListener('input', debounce((e) => {
            saveNote(a.id, e.target.value);
            if (e.target.value.trim()) noteBtn.classList.add('active');
            else noteBtn.classList.remove('active');
        }, 500));

        if (a.title.includes('Статья 65')) {
            const mapBtn = document.createElement('button');
            mapBtn.className = 'btn btn-primary';
            mapBtn.style.marginTop = '10px';
            mapBtn.textContent = '🗺️ Открыть карту РФ';
            mapBtn.onclick = () => $('#mapDialog').showModal();
            $('.body', node).appendChild(mapBtn);
        }

        container.append(node);
    });

    initDynamicEvents(container);
}

let _shareTitle = '';

function cleanHtmlToText(rawHtml) {
    return rawHtml
        .replace(/<\/p>\s*<p[^>]*>/gi, '\n')
        .replace(/<br\s*\/?>/gi, '\n')
        .replace(/<[^>]+>/g, '')
        .replace(/[ \t]+/g, ' ')
        .replace(/\n{3,}/g, '\n\n')
        .trim();
}

function openShareDialog(title, text) {
    const dlg = $('#shareDialog');
    const canvas = $('#shareCanvas');
    if (!dlg || !canvas) return;

    _shareTitle = title;
    const bodyText = cleanHtmlToText(text);

    const quoteEdit = $('#quoteEditText');
    if (quoteEdit) quoteEdit.value = bodyText;

    generateQuoteImage(canvas, title, bodyText);
    dlg.showModal();

    safeAddListener('#closeShare', 'click', () => dlg.close());

    $('#regenerateQuoteBtn').onclick = () => {
        const txt = ($('#quoteEditText')?.value || '').trim();
        if (txt) generateQuoteImage(canvas, _shareTitle, txt);
    };

    $('#downloadImgBtn').onclick = () => {
        const link = document.createElement('a');
        link.download = `quote-${Date.now()}.png`;
        link.href = canvas.toDataURL();
        link.click();
    };

    $('#shareNativeBtn').onclick = () => {
        canvas.toBlob(blob => {
            const file = new File([blob], 'quote.png', { type: 'image/png' });
            if (navigator.share && navigator.canShare && navigator.canShare({ files: [file] })) {
                navigator.share({ files: [file], title: 'Конституция РФ', text: _shareTitle }).catch(console.error);
            } else if (navigator.share) {
                navigator.share({ title: 'Конституция РФ', text: `${_shareTitle}\n${$('#quoteEditText')?.value || ''}` }).catch(console.error);
            } else {
                showToast('Ваш браузер не поддерживает отправку');
            }
        });
    };
}

// Вспомогательная функция: разбивает текст на строки с переносом
function wrapToLines(ctx, text, maxWidth) {
    const lines = [];
    for (const para of text.split('\n')) {
        const trimmed = para.trim();
        if (!trimmed) { lines.push(''); continue; }
        const words = trimmed.split(' ');
        let line = '';
        for (const word of words) {
            const test = line ? line + ' ' + word : word;
            if (ctx.measureText(test).width > maxWidth && line) {
                lines.push(line);
                line = word;
            } else {
                line = test;
            }
        }
        if (line) lines.push(line);
    }
    return lines;
}

function generateQuoteImage(canvas, title, text) {
    // --- Константы тетради ---
    const CELL     = 26;        // размер клетки в px
    const W        = 820;       // ширина листа (≈ A5 landscape)
    const MARG_L   = 80;        // красная полоса (левый отступ)
    const MARG_R   = 40;        // правый отступ
    const MARG_T   = CELL * 3;  // верхний отступ (3 клетки)
    const MARG_B   = CELL * 3;  // нижний отступ
    const FONT_SZ  = 20;        // размер шрифта (влезет в клетку CELL=26)
    const FONT     = `${FONT_SZ}px "Georgia","Times New Roman",serif`;
    const FONT_B   = `bold ${FONT_SZ}px "Georgia","Times New Roman",serif`;
    const TEXT_W   = W - MARG_L - MARG_R - 10;

    // --- Временный контекст для измерений ---
    const tmp = document.createElement('canvas');
    tmp.width = W; tmp.height = 10;
    const tc = tmp.getContext('2d');

    tc.font = FONT_B;
    const titleLines = wrapToLines(tc, title, TEXT_W);

    tc.font = FONT;
    const bodyLines = wrapToLines(tc, String(text || '').trim(), TEXT_W);

    // Итоговый список строк: заголовок, пустая, текст статьи, пустая
    const allLines = [...titleLines, '', ...bodyLines];

    // --- Высота холста ---
    const H = MARG_T + allLines.length * CELL + MARG_B;
    canvas.width  = W;
    canvas.height = H;

    const ctx = canvas.getContext('2d');

    // === ФОНОВАЯ БУМАГА ===
    ctx.fillStyle = '#fdf8ee'; // тёплый кремовый
    ctx.fillRect(0, 0, W, H);

    // Лёгкая тень по краям (книжный эффект)
    const shadow = ctx.createLinearGradient(0, 0, 18, 0);
    shadow.addColorStop(0, 'rgba(0,0,0,0.07)');
    shadow.addColorStop(1, 'rgba(0,0,0,0)');
    ctx.fillStyle = shadow;
    ctx.fillRect(0, 0, 18, H);
    const shadow2 = ctx.createLinearGradient(W - 18, 0, W, 0);
    shadow2.addColorStop(0, 'rgba(0,0,0,0)');
    shadow2.addColorStop(1, 'rgba(0,0,0,0.05)');
    ctx.fillStyle = shadow2;
    ctx.fillRect(W - 18, 0, 18, H);

    // === КЛЕТКА ===
    ctx.strokeStyle = 'rgba(130, 190, 220, 0.5)';
    ctx.lineWidth = 0.8;
    // Горизонтальные
    for (let y = CELL; y < H; y += CELL) {
        ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(W, y); ctx.stroke();
    }
    // Вертикальные
    for (let x = CELL; x < W; x += CELL) {
        ctx.beginPath(); ctx.moveTo(x, 0); ctx.lineTo(x, H); ctx.stroke();
    }

    // === КРАСНАЯ ПОЛОСА (поля) ===
    ctx.strokeStyle = 'rgba(215, 55, 55, 0.5)';
    ctx.lineWidth = 1.8;
    ctx.beginPath(); ctx.moveTo(MARG_L, 0); ctx.lineTo(MARG_L, H); ctx.stroke();

    // === ТЕКСТ ===
    ctx.textBaseline = 'alphabetic';
    ctx.textAlign = 'left';
    // Первая строка: снизу первой клетки отступа + шрифт
    let y = MARG_T;

    for (let i = 0; i < allLines.length; i++) {
        const line = allLines[i];
        if (!line) { y += CELL; continue; }
        const isTitleLine = i < titleLines.length;
        ctx.font = isTitleLine ? FONT_B : FONT;
        ctx.fillStyle = isTitleLine ? '#1a237e' : '#1c1c2e';
        ctx.fillText(line, MARG_L + 8, y);
        y += CELL;
    }

    // === ДИАГОНАЛЬНЫЙ ВОДЯНОЙ ЗНАК ===
    ctx.save();
    ctx.globalAlpha = 0.15;
    ctx.fillStyle = '#1a237e';
    ctx.font = 'bold 18px sans-serif';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    const wmAngle = -Math.PI / 6; // -30°
    const wmSpX = 200;
    const wmSpY = 80;
    for (let wx = -wmSpX; wx < W + wmSpX; wx += wmSpX) {
        for (let wy = -wmSpY; wy < H + wmSpY; wy += wmSpY) {
            ctx.save();
            ctx.translate(wx, wy);
            ctx.rotate(wmAngle);
            ctx.fillText('PrepMate', 0, 0);
            ctx.restore();
        }
    }
    ctx.restore();

    // === ШТАМП PrepMate (нижний правый угол) ===
    ctx.fillStyle = 'rgba(26,35,126,0.82)';
    ctx.font = 'italic bold 14px sans-serif';
    ctx.textAlign = 'right';
    ctx.textBaseline = 'alphabetic';
    ctx.fillText('PrepMate — Конституция РФ', W - 12, H - 8);
}

// Считает количество строк (с учётом \n как принудительного переноса)
function countLines(ctx, text, maxWidth) {
    let count = 0;
    for (const para of text.split('\n')) {
        const words = para.trim().split(' ').filter(Boolean);
        if (!words.length) { count++; continue; }
        let line = '';
        for (const word of words) {
            const testLine = line + word + ' ';
            if (ctx.measureText(testLine).width > maxWidth && line) {
                count++;
                line = word + ' ';
            } else {
                line = testLine;
            }
        }
        count++;
    }
    return count;
}

// Рисует текст с переносами по словам и по \n (с отступом между параграфами)
function wrapTextCentered(ctx, text, x, y, maxWidth, lineHeight) {
    const paras = text.split('\n');
    for (let p = 0; p < paras.length; p++) {
        if (p > 0) y += lineHeight * 0.45; // дополнительный отступ между пунктами
        const words = paras[p].trim().split(' ').filter(Boolean);
        if (!words.length) continue;
        let line = '';
        for (const word of words) {
            const testLine = line + word + ' ';
            if (ctx.measureText(testLine).width > maxWidth && line) {
                ctx.fillText(line.trim(), x, y);
                line = word + ' ';
                y += lineHeight;
            } else {
                line = testLine;
            }
        }
        if (line.trim()) { ctx.fillText(line.trim(), x, y); y += lineHeight; }
    }
    return y - lineHeight;
}

function initDynamicEvents(container) {
    container.querySelectorAll('.cross-link').forEach(link => {
        link.addEventListener('click', (e) => {
            e.preventDefault(); e.stopPropagation();
            state.returnPosition = window.scrollY; state.isJumping = true;
            const targetId = link.dataset.target;
            const el = document.getElementById(targetId);
            if (el) {
                const offset = 80;
                const elementPosition = el.getBoundingClientRect().top + window.scrollY;
                window.scrollTo({ top: elementPosition - offset, behavior: "smooth" });
                state.landingPosition = elementPosition - offset;
                showReturnButton();
                el.classList.add('highlight');
                setTimeout(() => { el.classList.remove('highlight'); state.isJumping = false; }, 1000);
            }
        });
    });
}

function moveTooltip(e) {
    const tooltip = $('#tooltip'); if (!tooltip) return;
    const x = e.clientX; const y = e.clientY;
    tooltip.style.left = (x + 15) + 'px'; tooltip.style.top = (y + 15) + 'px';
    if (x + 320 > window.innerWidth) tooltip.style.left = (x - 315) + 'px';
    if (y + 100 > window.innerHeight) tooltip.style.top = (y - 100) + 'px';
}

function showToast(msg = "Ссылка скопирована!") {
    const toast = $('#toast'); if (!toast) return;
    toast.textContent = msg;
    toast.classList.add("show");
    setTimeout(() => { toast.classList.remove("show"); }, 3000);
}

function initDictionary() {
    safeAddListener('#dictionaryBtn', 'click', () => {
        const dlg = $('#dictionaryDialog'); if (!dlg) return;
        const list = $('#dictionaryList');
        if (list && list.innerHTML === '') {
            Object.keys(DICTIONARY).sort().forEach(term => {
                const div = document.createElement('div');
                div.className = 'dict-item';
                div.innerHTML = `<span class="dict-term">${term.charAt(0).toUpperCase() + term.slice(1)}</span><span class="dict-def">${DICTIONARY[term]}</span>`;
                list.appendChild(div);
            });
        }
        dlg.showModal();
    });
    safeAddListener('#closeDictionary', 'click', () => $('#dictionaryDialog').close());
}


/* --- IMPROVED MAP LOGIC (ZOOM) --- */
function initMap() {
    safeAddListener('#closeMap', 'click', () => $('#mapDialog').close());
    const title = $('#mapRegionTitle');
    const list = $('#mapRegionList');
    const zoomLayer = $('#zoomLayer');

    // Zoom Controls
    safeAddListener('#zoomIn', 'click', () => changeZoom(0.3));
    safeAddListener('#zoomOut', 'click', () => changeZoom(-0.3));
    safeAddListener('#zoomReset', 'click', () => { state.mapZoom = 1; state.mapPan = {x:0,y:0}; updateMapTransform(); });

    // Click on region
    $$('.region').forEach(reg => {
        reg.addEventListener('click', (e) => {
            const id = e.target.id;
            const data = FEDERAL_DISTRICTS[id];
            if (data) {
                title.textContent = data.title;
                list.innerHTML = '';
                const items = data.list.split(',').map(s => s.trim());
                items.forEach(i => {
                    const li = document.createElement('li');
                    li.innerHTML = `<span class="flag-placeholder"></span> ${i}`;
                    list.appendChild(li);
                });
            }
        });
    });

    // Drag Logic
    const container = $('#mapContainer');
    let isDragging = false;
    let startX, startY;

    container.addEventListener('mousedown', e => { isDragging = true; startX = e.clientX - state.mapPan.x; startY = e.clientY - state.mapPan.y; });
    window.addEventListener('mouseup', () => isDragging = false);
    container.addEventListener('mousemove', e => {
        if (!isDragging) return;
        e.preventDefault();
        state.mapPan.x = e.clientX - startX;
        state.mapPan.y = e.clientY - startY;
        updateMapTransform();
    });
}

function changeZoom(delta) {
    state.mapZoom = Math.max(1, Math.min(3, state.mapZoom + delta));
    updateMapTransform();
}

function updateMapTransform() {
    const layer = $('#zoomLayer');
    if (layer) {
        layer.style.transform = `translate(${state.mapPan.x}px, ${state.mapPan.y}px) scale(${state.mapZoom})`;
    }
}

function closeMobileExtras() {
    $('#mobileToolsSheet').hidden = true;
    $('#sidebarPanel')?.classList.remove('visible');
    const overlay = $('#mobileSearchOverlay');
    if (overlay) overlay.hidden = true;
}

function initMobileNav() {
    let _lastHomeClick = 0;

    safeAddListener('#navHome', 'click', () => {
        const now = Date.now();
        const isDoubleTap = (now - _lastHomeClick) < 600;
        _lastHomeClick = now;

        closeMobileExtras();
        if (state.showFavoritesOnly) setFavFilterMode();

        if (isDoubleTap && state.activeSearchQuery) {
            // Двойной тап — сбросить поиск и показать все статьи
            filterArticles('');
            const si = $('#searchInput');
            if (si) si.value = '';
            const mi = $('#mobileSearchInput');
            if (mi) mi.value = '';
            showToast('Поиск сброшен');
        }

        scrollToTop();
        $$('.nav-item').forEach(b => b.classList.remove('active'));
        $('#navHome').classList.add('active');
    });

    safeAddListener('#navSearch', 'click', () => {
        closeMobileExtras();
        if (state.showFavoritesOnly) setFavFilterMode();
        $$('.nav-item').forEach(b => b.classList.remove('active'));
        $('#navSearch').classList.add('active');
        const overlay = $('#mobileSearchOverlay');
        overlay.hidden = false;
        const mobileInput2 = $('#mobileSearchInput');
        if (mobileInput2) mobileInput2.value = '';
        showMobileSearchHistory();
        setTimeout(() => mobileInput2?.focus(), 100);
    });

    safeAddListener('#navFav', 'click', () => {
        closeMobileExtras();
        setFavFilterMode();
        $$('.nav-item').forEach(b => b.classList.remove('active'));
        if (state.showFavoritesOnly) $('#navFav').classList.add('active');
    });

    safeAddListener('#navMenu', 'click', () => {
        $('#mobileToolsSheet').hidden = true;
        const overlay = $('#mobileSearchOverlay');
        if (overlay) overlay.hidden = true;
        $('#sidebarPanel').classList.toggle('visible');
        $$('.nav-item').forEach(b => b.classList.remove('active'));
        if ($('#sidebarPanel').classList.contains('visible')) $('#navMenu').classList.add('active');
    });

    safeAddListener('#navTools', 'click', () => {
        const sheet = $('#mobileToolsSheet');
        if (!sheet) return;
        // Просто переключаем sheet — ничего больше не трогаем.
        // Sheet имеет z-index 4700 → появляется поверх поиска, меню, избранного.
        sheet.hidden = !sheet.hidden;
    });

    // Кнопки в sheet — дублируем функции из header-tools
    safeAddListener('#mobileFlashcards', 'click', () => { $('#mobileToolsSheet').hidden = true; $('#flashcardsBtn')?.click(); });
    safeAddListener('#mobileDictionary', 'click', () => { $('#mobileToolsSheet').hidden = true; $('#dictionaryBtn')?.click(); });
    safeAddListener('#mobileGame23', 'click', () => { $('#mobileToolsSheet').hidden = true; $('#game23Btn')?.click(); });
    safeAddListener('#mobileGame', 'click', () => { $('#mobileToolsSheet').hidden = true; $('#gameBtn')?.click(); });
    safeAddListener('#mobileFontBtn', 'click', () => { $('#mobileToolsSheet').hidden = true; $('#fontBtn')?.click(); });
    safeAddListener('#mobileTheme', 'click', () => { $('#mobileToolsSheet').hidden = true; toggleTheme(); });

    // Закрытие мобильного поиска
    safeAddListener('#mobileSearchClose', 'click', () => {
        $('#mobileSearchOverlay').hidden = true;
        $$('.nav-item').forEach(b => b.classList.remove('active'));
        $('#navHome').classList.add('active');
    });

    // Поиск при вводе (с задержкой)
    const mobileInput = $('#mobileSearchInput');
    if (mobileInput) {
        mobileInput.addEventListener('input', debounce((e) => {
            const q = e.target.value.trim();
            const container = $('#mobileSearchResults');
            if (!q) { showMobileSearchHistory(); return; }
            const results = state.articles.filter(a => {
                const t = a.title.toLowerCase();
                const b = a.bodyHTML.replace(/<[^>]+>/g, ' ').toLowerCase();
                return t.includes(q.toLowerCase()) || b.includes(q.toLowerCase());
            }).slice(0, 15);
            container.innerHTML = results.length
                ? results.map(a => `<div class="mobile-search-result-item" data-id="${a.id}" data-title="${a.title}"><strong>${a.title}</strong><span style="color:var(--muted);font-size:12px;margin-left:6px">${a.chapterTitle}</span></div>`).join('')
                : '<div style="padding:20px;text-align:center;color:var(--muted)">Ничего не найдено</div>';
            container.querySelectorAll('.mobile-search-result-item').forEach(el => {
                el.addEventListener('click', () => {
                    // Сохраняем название выбранной статьи, а не текст запроса
                    saveSearchQuery(el.dataset.title);
                    $('#mobileSearchOverlay').hidden = true;
                    $$('.nav-item').forEach(b => b.classList.remove('active'));
                    state.showFavoritesOnly = false;
                    renderArticles(state.articles);
                    setTimeout(() => {
                        const target = document.getElementById(el.dataset.id);
                        if (target) { target.scrollIntoView({ behavior: 'smooth', block: 'center' }); target.classList.add('highlight'); setTimeout(() => target.classList.remove('highlight'), 1500); }
                    }, 300);
                });
            });
        }, 300));
        mobileInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter') {
                const q = mobileInput.value.trim();
                if (q) {
                    saveSearchQuery(q);
                    $('#mobileSearchOverlay').hidden = true;
                    $$('.nav-item').forEach(b => b.classList.remove('active'));
                    filterArticles(q);
                }
            }
        });
    }
}

function showMobileSearchHistory() {
    const container = $('#mobileSearchResults');
    if (!container) return;
    if (!state.searchHistory || !state.searchHistory.length) {
        container.innerHTML = '<div style="padding:30px 20px;text-align:center;color:var(--muted);font-size:14px">Начните вводить запрос...</div>';
        return;
    }
    container.innerHTML =
        '<div style="padding:6px 4px 8px;color:var(--muted);font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.7px">Недавние запросы</div>' +
        state.searchHistory.map(q =>
            `<div class="mobile-search-result-item mobile-history-item" data-q="${q}" style="display:flex;align-items:center;gap:10px">
                <span style="font-size:16px;opacity:0.5">🕐</span>
                <span>${q}</span>
            </div>`
        ).join('');
    container.querySelectorAll('.mobile-history-item').forEach(el => {
        el.addEventListener('click', () => {
            const q = el.dataset.q;
            $('#mobileSearchInput').value = q;
            $('#mobileSearchOverlay').hidden = true;
            $$('.nav-item').forEach(b => b.classList.remove('active'));
            filterArticles(q);
        });
    });
}

/* --- INIT FOLDERS UI HANDLERS --- */
function initFoldersUI() {
    const select = $('#folderSelectFilter');
    if (select) {
        select.addEventListener('change', (e) => {
            state.currentFolderFilter = e.target.value;
            renderArticles();
        });
    }
}

function initPWAInstall() {
    let deferredPrompt;
    const btn = $('#installBtn');

    window.addEventListener('beforeinstallprompt', (e) => {
        e.preventDefault();
        deferredPrompt = e;
        btn.hidden = false; // Show button in header
    });

    btn.addEventListener('click', async () => {
        if (deferredPrompt) {
            deferredPrompt.prompt();
            const { outcome } = await deferredPrompt.userChoice;
            deferredPrompt = null;
            btn.hidden = true;
        }
    });
}

function initEvents() {
    safeAddListener('#themeToggle', 'click', toggleTheme);
    safeAddListener('#printBtn', 'click', () => window.print());
    safeAddListener('#favFilterBtn', 'click', setFavFilterMode);
    safeAddListener('#closeDialog', 'click', () => $('#articleDialog').close());
    
    $$('dialog').forEach(dlg => {
        dlg.addEventListener('click', (e) => {
            const rect = dlg.getBoundingClientRect();
            if (e.clientX < rect.left || e.clientX > rect.right || 
                e.clientY < rect.top || e.clientY > rect.bottom) {
                dlg.close();
            }
        });
    });

    const searchInput = $('#searchInput');
    const searchBtn = $('#searchTriggerBtn');
    if (searchInput) searchInput.addEventListener('keydown', e => { if (e.key === 'Enter') performSearch(searchInput.value); });
    if (searchBtn) searchBtn.addEventListener('click', () => { if (searchInput) performSearch(searchInput.value); });

    safeAddListener('#backToTop', 'click', scrollToTop);
    safeAddListener('#btn-return', 'click', returnBack);

    const content = $('#content');
    if (content) {
        content.addEventListener('click', e => {
            if (e.target.closest('.player-btn') || e.target.closest('.sheet-btn')) return;

            if (e.target.closest('.term') || e.target.closest('.cross-link') || e.target.closest('button') || e.target.closest('.note-area') || e.target.closest('select')) return;
            const card = e.target.closest('.card');
            if (card && e.altKey) openDialogById(card.dataset.articleId);
        });
        content.addEventListener('dblclick', e => {
            if (e.target.closest('.term') || e.target.closest('.cross-link') || e.target.closest('button') || e.target.closest('.note-area') || e.target.closest('select')) return;
            const card = e.target.closest('.card');
            if (card) openDialogById(card.dataset.articleId);
        });
    }

    window.addEventListener('hashchange', () => {
        const hash = location.hash.replace('#', ''); if (!hash) return;
        const target = document.getElementById(hash);
        if (target) { target.scrollIntoView({ behavior: 'smooth', block: 'center' }); target.classList.add('highlight'); setTimeout(() => target.classList.remove('highlight'), 1500); }
    });
    window.addEventListener('scroll', updateScrollState);
    window.addEventListener('keydown', e => {
        if (e.key === '/' && document.activeElement.tagName !== 'INPUT' && document.activeElement.tagName !== 'TEXTAREA') { e.preventDefault(); $('#searchInput').focus(); }
    });
}

function initServiceWorker() {
    if ('serviceWorker' in navigator) {
        let refreshing;
        navigator.serviceWorker.addEventListener('controllerchange', () => {
            if (refreshing) return;
            window.location.reload();
            refreshing = true;
        });

        navigator.serviceWorker.register('./sw.js').then(reg => {
            reg.update(); 
            const showUpdateUI = (worker) => {
                const toast = $('#updateNotification');
                const btn = $('#reloadBtn');
                if (toast && btn) {
                    toast.hidden = false;
                    btn.onclick = () => {
                        btn.disabled = true;
                        btn.textContent = 'Обновление...';
                        worker.postMessage({ type: 'SKIP_WAITING' });
                    };
                }
            };
            if (reg.waiting) { showUpdateUI(reg.waiting); return; }
            reg.addEventListener('updatefound', () => {
                const newWorker = reg.installing;
                newWorker.addEventListener('statechange', () => {
                    if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
                        showUpdateUI(newWorker);
                    }
                });
            });
        }).catch(err => console.error('SW Error:', err));
    }
}

/* --- ЗАЩИЩЕННАЯ ЗАГРУЗКА --- */
async function loadChapters() {
    const container = $('#content');
    const cachedData = localStorage.getItem(LS.CACHE_CHAPTERS);
    
    // 1. Попытка показать кэш (для скорости)
    if (cachedData) {
        try {
            state.articles = JSON.parse(cachedData);
            renderArticles(); 
            buildTOC();
        } catch (e) { console.error('Ошибка кэша:', e); }
    }

    try {
        const files = [
            'chapters/chapter1.html', 'chapters/chapter2.html', 'chapters/chapter3.html',
            'chapters/chapter4.html', 'chapters/chapter5.html', 'chapters/chapter6.html',
            'chapters/chapter7.html', 'chapters/chapter8.html', 'chapters/chapter9.html'
        ];
        
        // Fetch с тайм-аутом, чтобы не висело вечно
        const fetchWithTimeout = (url, timeout = 5000) => {
            return Promise.race([
                fetch(url).then(r => {
                    if (!r.ok) throw new Error(`HTTP ${r.status}`);
                    return r.text();
                }),
                new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), timeout))
            ]);
        };

        const results = await Promise.allSettled(files.map(f => fetchWithTimeout(f)));

        let newArticles = [];
        const parser = new DOMParser();

        results.forEach((res, index) => {
            if (res.status === 'fulfilled') {
                const html = res.value;
                const doc = parser.parseFromString(html, 'text/html');
                const chapterTitle = doc.querySelector('h2')?.textContent?.trim() || `Глава ${index + 1}`;
                
                // Ищем статьи. Если формат сбит, пробуем более широкий поиск
                const articlesNodes = doc.querySelectorAll('article.interactive-article, article, div.article');
                
                articlesNodes.forEach(artNode => {
                    const id = artNode.id || `article-${index}-${Math.random().toString(36).slice(2, 7)}`;
                    const titleHeader = artNode.querySelector('h3') || artNode.querySelector('h4') || artNode.querySelector('strong');
                    const title = artNode.getAttribute('data-title') || titleHeader?.textContent?.trim() || 'Статья';
                    
                    // Парсинг пояснений
                    let explain = artNode.getAttribute('data-comment') || '';
                    const explainNode = artNode.querySelector('.explanation-source');
                    if (explainNode) {
                        explain = explainNode.innerHTML;
                        // Не удаляем узел из DOM сразу, чтобы не ломать клонирование ниже, если что
                        // но для bodyHTML нужно удалить
                    }

                    // Клонируем для bodyHTML
                    const bodyClone = artNode.cloneNode(true);
                    // Удаляем заголовок из тела, чтобы не дублировался
                    const h3 = bodyClone.querySelector('h3, h4');
                    if(h3) h3.remove();
                    
                    // Удаляем блок с пояснением из тела
                    const expSource = bodyClone.querySelector('.explanation-source');
                    if(expSource) expSource.remove();

                    if (bodyClone.textContent.trim().length > 0) {
                         newArticles.push({ id, title, bodyHTML: bodyClone.innerHTML, explainHTML: explain, chapterTitle });
                    }
                });
            } else {
                console.warn(`Пропущена глава ${index + 1}:`, res.reason);
            }
        });

        if (newArticles.length > 0) {
            state.articles = newArticles;
            try { localStorage.setItem(LS.CACHE_CHAPTERS, JSON.stringify(newArticles)); } catch (e) { }
            
            // Безопасный рендер
            try { renderArticles(); } catch(e) { console.error('Render error:', e); }
            try { buildTOC(); } catch(e) { console.error('TOC error:', e); }
            
            applyFontSettings();
        } else if (!cachedData) {
            throw new Error("Не удалось загрузить ни одной главы (пустой результат).");
        }
        
    } catch (e) {
        console.error('Critical load error:', e);
        if (state.articles.length === 0 && container) {
            container.innerHTML = `<div class="error" style="color:red;padding:20px;border:1px solid red;text-align:center">
                <h3>Ошибка загрузки</h3>
                <p>${e.message}</p>
                <button onclick="location.reload()" class="btn">Попробовать еще раз</button>
            </div>`;
        }
    } finally {
        // Снимаем лоадер ВСЕГДА, даже если была ошибка
        if (container) container.classList.remove('loading');
        updateScrollState();
    }
}

function openDialogById(id) {
    const art = state.articles.find(x => x.id === id); if (!art) return;
    const dlg = $('#articleDialog'); if (!dlg) return;
    $('#dialogTitle').textContent = art.title;
    $('#dialogBody').innerHTML = processText(art.bodyHTML) + (art.explainHTML ? `<hr><div class="muted">Пояснение:</div>${processText(art.explainHTML)}` : '');
    initDynamicEvents($('#dialogBody'));
    dlg.showModal();
}

function boot() {
    applyTheme(true);
    loadFavorites(); 
    loadNotes(); 
    initFontSettings(); 
    initSearchHistory(); 
    initTimer(); 
    initGame(); 
    initGame23(); 
    initFlashcards(); 
    initDictionary(); 
    initMap(); 
    initMobileNav(); 
    initFoldersUI(); 
    initEvents();
    initSpyScroll();
    // initContextMenu(); // Функция не найдена в исходнике, закомментировал
    initPWAInstall();
    initServiceWorker();
    loadChapters();
}

document.addEventListener('DOMContentLoaded', boot);